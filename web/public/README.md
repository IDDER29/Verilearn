# `web/public` — static assets

Files served verbatim at the site root by Next.js. A file here at `public/foo.svg` is reachable at `/foo.svg`.

Currently just the default framework SVGs (`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`). Add favicons, images, fonts, or other static assets here.
