# `web/components/review` — review session controls

Interactive pieces of the review flow (`/review/*`).

| File | What it is |
|---|---|
| `DiscussPanel.tsx` | The "Discuss this answer" panel (`/review/discuss`) — shows the real card/claim the learner tapped, its live trust state and source, and offers "Raise a conflict" / "Track as a gap" via the same real actions the workspace uses (REVIEW-08). The Skeptic's reply is a labeled sample pending the deferred LLM. |
