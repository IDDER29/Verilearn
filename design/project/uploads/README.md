# `design/project/uploads` — prototype assets

Images pasted into the design tool during the prototype's creation (`pasted-*.png`) and a combined single-file export (`verilearn_prototype_v2.html`). Referenced by the prototypes in the parent folder. Frozen reference — part of the [design archive](../..), not used by the app.
