# `design/chats` — design-tool transcripts

The conversation(s) from the design tool that produced the prototypes in [`../project`](../project). This is where the *intent* lives — the back-and-forth showing what the user asked for and where they landed after iterating, which the final HTML files are only the output of.

| File | What it is |
|---|---|
| `chat1.md` | The design conversation transcript. |

Frozen reference. Useful when you need the *why* behind a design decision that the prototype alone doesn't explain.
