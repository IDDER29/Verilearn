### Topic Creation & Verification Pipeline (VERIFY)

| Story | Status | Evidence / Justification |
| --- | --- | --- |
| VERIFY-01 | ✅ Done | `/new-topic` (`web/app/new-topic/page.tsx`) wires the 3 ordered fields (topic / "where you're starting from" / goal pills) to `createTopicAction` → `createTopic` (`web/lib/services/topics.ts`); oversized paste truncated by `sanitizeTopicInput`/`MAX_TOPIC_LEN`. Minor deviations: goal has no "custom" option and the "What happens next" preview lists 5 stages (Decompose omitted). |
| VERIFY-02 | ✅ Done | New-topic `ready` gate disables submit; `tried` state shows per-field hints; button flips to "Ready to verify → Start verifying"; `submitting` guard makes submit idempotent; `serverError` renders inline retry. Server re-checks title (`createTopic` `title.length < 2`) and triage rejects a thin level, though the server gate is lighter than the client's. |
| VERIFY-03 | ✅ Done | "Popular:" row in `new-topic/page.tsx` pre-fills field 1 (TCP/IP, Binary trees, Bloom filters); picks flow through the identical `ready` gate and `createTopic` pipeline — no shortcut around verification. |
| VERIFY-04 | 🟡 Partial | `web/app/pipeline/page.tsx` renders an animated stage stepper with Done/Running/Waiting text pills, % ring and elapsed timer. But it is a client-side timed simulation (`STEP_MS` timers) with canned detail copy, lists only 5 stages (Decompose missing), and is NOT wired to the real `runPipeline` output — the engine emits real per-stage `StageResult` (`web/lib/domain/pipeline.ts`) that the UI never consumes. |
| VERIFY-05 | 🟡 Partial | `createTopic` runs the pipeline server-side (in the service, independent of the tab) and stamps `status` ready/verifying. But it runs synchronously in-process — no durable/resumable background job, no library "verifying" polling, and no ready notification/badge. "You can leave this page" copy on the pipeline screen is static. Needs durable async job orchestration. |
| VERIFY-06 | ✅ Done | `createTopic` enforces the Free 3-topic cap server-side BEFORE compute (`user.plan === "free" && existing.length >= 3`) returning the upgrade error, surfaced as `serverError` in the UI. Synchronous in-memory store makes it atomic. Missing: archive/delete alternative in the nudge copy. |
| VERIFY-07 | ✅ Done | Triage stage in `runPipeline` + `DeterministicVerifier.triage` outputs level anchor + bounded section count and refuses nonsense/too-thin specs, stopping before retrieval. Tests: pipeline.test.ts "refuses at triage on a nonsense/too-broad spec and stops", "triage rejects a too-thin level". |
| VERIFY-08 | 🟡 Partial | Retrieve stage implemented and tested — builds a typed source pool, reports count, and feeds `resolvedIds` to the hallucination guard. But real citable-source retrieval is the deferred LLM/corpus adapter (`DeterministicVerifier.retrieveSources` is a 2-source stand-in), and sources are not persisted onto the `TopicRecord` (`sources: []` in `createTopic`), so no coverage-matrix column survives. |
| VERIFY-09 | 🟡 Partial | Teach stage exists and is tested (drafts sections pitched to the triaged level, reports "N sections drafted"). But it emits placeholder section titles, not honest confidence-flagging prose — real lecture authoring is the deferred LLM adapter behind the `Verifier` port. |
| VERIFY-10 | ✅ Done | Decompose stage yields atomic claims (one row per section via `DeterministicVerifier.decomposeClaims`), and the core invariant "every decomposed claim ends tagged" is tested (pipeline.test.ts: `r.states.size === r.claims.length`, "decomposes one atomic claim per section"). Real decomposition quality is the deterministic stand-in. |
| VERIFY-11 | ✅ Done | Verify stage matches each claim → draft `verified_source`/`sourced`/`unsupported`, records via the ledger firewall, reports "N/M claims matched"; `pipelineAuthorityClamp` prevents the pipeline minting Verified·execution (tested). Sandbox routing for computational claims is deferred (see VERIFY-12). |
| VERIFY-12 | ⏭️ Deferred | Real Execution Sandbox (deterministic, resource-bounded, network-isolated) needs external infra; `DeterministicVerifier` stands in. The authority-boundary seam is built and tested: `pipelineAuthorityClamp` fails a stray `verified_execution` verdict closed to Unsupported ("the pipeline cannot mint it (sandbox only)"), and `verified_execution` exists in the trust-state types. |
| VERIFY-13 | ✅ Done | Skeptic stage proposes Disputed/Interpretive without finalizing (writes only on change; SME disposes). Canonical catch tested: pipeline.test.ts "flags an over-broad claim as disputed" (Dijkstra / "any weighted graph"); "fails cleanly ... when the Skeptic stalls"; reports "N claims disputed · flagged, not hidden". Real Skeptic LLM deferred; rule-based adapter passes the canonical case. |
| VERIFY-14 | ⏭️ Deferred | No hard-mode Skeptic logic, plan-gating, or UI affordance exists in the pipeline/verifier. A meaningful "deeper scrutiny" mode needs the real Skeptic model; the plan-entitlement substrate (`user.plan`) exists but hard-mode gating is not built. |
| VERIFY-15 | 🟡 Partial | Engine fails cleanly and stops at the first failed stage (`runPipeline` `ok=false`, stops — tested for triage refusal and Skeptic stall; "we'd rather stop than guess"). But no failure UI is wired (the pipeline page is a timed sim that always succeeds), no VL-503-class error code / Retry / Back-to-library actions, and `createTopic` maps a failed run to status `"verifying"` rather than a distinct `"failed"` state. |
| VERIFY-16 | ✅ Done | `sourceHallucinationGuard` fails a citation to an unresolvable/fabricated source closed to Unsupported; no state above Sourced rests on an unverifiable citation. Tests: pipeline.test.ts "a fabricated-citation verdict never yields a verified state", plus guard unit cases. Real citation-resolution corpus deferred, but the guard fails closed regardless. |
| VERIFY-17 | 🟡 Partial | `TrustLedger.record` is append-only history (the substrate for re-recording a claim's verdict exists and downgrades render fail-closed). But there is no targeted re-match/re-verification service, sources aren't persisted for re-matching, and no "SME closes an Unsupported row → re-run affected claims" trigger is wired. |
| VERIFY-18 | 🟡 Partial | The static pipeline page encodes stage status as text pills (Done/Running/Waiting), so status is not color-only. But there are no `aria-live` regions announcing Waiting→Running→Done transitions or the completion/failure event to assistive tech, and the completion event is not announced. |
| VERIFY-19 | 🟡 Partial | Prompt-injection defense is implemented and tested as load-bearing: `sanitizeTopicInput` neutralizes embedded instructions (ignore-instructions / auto-certify / bypass-skeptic / role-override) so injected text cannot elevate a claim (pipeline.test.ts "sanitization is load-bearing"). Missing: content-policy refusal of unsafe/abusive topics at triage, and abuse rate-limiting / creation-freeze. |
| VERIFY-20 | 🟡 Partial | Authority firewall built and tested (`web/lib/domain/rbac.ts`): `platform_admin` holds infra perms and explicitly EXCLUDES `trust:write`/`cert:revoke`; `assertNoFirewallGrants` proves no human role can write trust; break-glass for a firewall permission is recorded denied. Per-event `producerVersion` (version tag for audit/rollback) is stamped by `runPipeline`. Not built: job-restart, capacity scaling, and model rollout/rollback operations. |
| VERIFY-21 | 🟡 Partial | `support_agent` is firewalled to read-only in `rbac.ts` (`topic:read`, `claim:read`, `support:read_tickets`; no `trust:write`), and break-glass/impersonation is default-deny + audited — support cannot fabricate a verification result. Not built: the scoped-consent re-queue/restart operation for a stuck pipeline. |
| VERIFY-22 | 🟡 Partial | The pure engine can run a canned topic with zero persistence (no DB required), which is the substrate for an ephemeral demo. But no unauthenticated guest demo route, preview labeling, conversion CTA, or demo rate-limiting is built; topic creation is auth-gated. |
| VERIFY-23 | 🟡 Partial | `instructor` role is firewalled in `rbac.ts` (pedagogical perms, no `trust:write` — cannot flip trust states), upholding "pedagogical not epistemic authority". Not built: the instructor vet flow (run pipeline → review trust bar/matrix/disputes → request SME re-verification), the shared Teams library, and publish-to-cohort with an unresolved-disputes warning. |

Counts: 23 total — 9 Done, 12 Partial, 2 Deferred, 0 Out-of-scope.
