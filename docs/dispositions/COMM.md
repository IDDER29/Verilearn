### Community, Contributions & Reputation (COMM)

Honest disposition of the Community domain against the actual codebase. The domain ships as two faithful **static** screens — `web/app/community/page.tsx` (thread list + hot debate + "Verified answers only" explainer + top-contributor leaderboard) and `web/app/community/[slug]/page.tsx` (thread with "✓ Verified answer / AI · sourced" block, human replies, upvote counts, Linked-claim chip). Neither is wired to a backend. The domain's core *invariants* (no human certifies, no self-certification, SME-gated promotion) are genuinely implemented and unit-tested in the engine (`web/lib/domain/trust.ts`, `web/lib/domain/rbac.ts`), but no community-side vote ledger, promotion-request pipeline, moderation/audit store, search, or DSAR flow exists.

| Story | Status | Evidence / Justification |
|-------|--------|--------------------------|
| COMM-01 | 🟡 Partial | Static landing exists: `web/app/community/page.tsx` renders hot-debate card, thread cards with topic anchor + type tags (`Disputed claim`/`Sources`/`Study group`), and the "🛡️ Verified answers only" explainer. Not wired to real threads; no first-run empty state and no retryable error state. |
| COMM-02 | 🟡 Partial | "New thread" affordance present (`page.tsx:95`) but it is a static button — no composer, no required topic anchor / optional claim-source anchoring, no anchor-ID resolution or persistence, no access-scope check. Surface only. |
| COMM-03 | 🟡 Partial | Static replies render in `[slug]/page.tsx`; no reply composer, no citation attach (URL/reference/topic-source), no "no source cited" marking, no optimistic post/reconcile. |
| COMM-04 | 🟡 Partial | Faithful static "✓ Verified answer" block with explicit "AI · sourced" badge and backing "Sourced from CLRS §24.3 · Lemma 24.15" (`[slug]/page.tsx:113,122,137`); human replies get no checkmark. The never-`Verified` invariant is backed by `trust.ts` (Skeptic/AI cannot emit a trust state via the firewall), but this block is hard-coded, not derived from a live source check; no "no source found"/"source check pending" states. |
| COMM-05 | 🟡 Partial | Thread statically shows a `Disputed claim` tag and a "Linked claim" chip deep-linking to the Conflict ("Open conflict · resolve →", `[slug]/page.tsx:201-209`). No "Dispute this claim" action that creates the thread, no live sync of trust/conflict status, no resolution reflection. |
| COMM-06 | ⏭️ Deferred | No "Nominate for sources" control and no promotion-request object exist. Needs the promotion-request pipeline + persistence. Seam exists on the SME side: `source:promote` permission in `rbac.ts` (tested `rbac.test.ts:78`). |
| COMM-07 | ⏭️ Deferred | No SME promotion inbox / intake. Needs the promotion-request queue + DB + audit trail. Seam: `sme_reviewer` role with `review:submit` + `source:promote` in `rbac.ts` (`rbac.test.ts:73-78`); actual source write is owned by the Conflicts/Trust/Sources domain. |
| COMM-08 | ⏭️ Deferred | No durable "promoted to sources" attribution/credit or reply badge. Needs promotion pipeline + persistent contributor profile + the source-entry origin field. Not implemented. |
| COMM-09 | 🟡 Partial | Upvote counts render statically (👍 42 / 👍 12 and per-reply likes in `page.tsx`/`[slug]/page.tsx`). No vote ledger, no one-vote-per-identity toggle, no server-side self-vote block, no idempotency/rate-limit, no recompute-from-ledger. |
| COMM-10 | 🟡 Partial | Static top-contributor leaderboard with medals and "helpful answers" counts (`page.tsx` CONTRIBUTORS 🥇🥈🥉). No score derived from received signals, no update on votes/promotions, no gaming suppression. The firewall (reputation must never feed learning signals) is respected only by absence — no computation exists yet. |
| COMM-11 | 🟡 Partial | `community_mod` role and `community:moderate` permission exist and are tested to grant no trust/ban power (`rbac.ts:159`, `rbac.test.ts:74`). No moderator tooling (re-anchor/retag/lock/close/route), no per-action logging, no UI. Role seam only. |
| COMM-12 | ⏭️ Deferred | No vote-ring / reputation-juking detection. Needs a vote ledger plus behavioral/graph analysis + freeze/void/revoke/ban infrastructure. Boundary is respected in `rbac.ts` (T&S roles never hold `trust:write`), but detection is unbuilt. |
| COMM-13 | 🟡 Partial | Core wall implemented + tested: `trust.ts` `EpistemicFirewallError` rejects any non-verifier, and `DualControlError` blocks self-approval (`smeApprovedBy === actor.id`, `trust.ts:64`); `rbac.ts` invariant I1 — NO human role holds `trust:write` — is tested for every role (`rbac.test.ts:95-105`). Missing: the community-side promotion-request object and its data-layer nominator≠approver rejection (no promotion request exists to enforce against). |
| COMM-14 | 🟡 Partial | Static community pages exist and a route auth-gate seam exists (`web/proxy.ts`). No guest-specific curated read-only view, no sign-up wall on post/vote/nominate actions, no guest rate-limiting or "no attribution" enforcement. |
| COMM-15 | ⏭️ Deferred | No shared-library threads. Depends on the Org/Teams domain (membership/roles) plus persistence. Seam: `store` OWNER/TENANT scoping (`web/lib/store/`) could scope visibility, but no thread entity or library routing exists. |
| COMM-16 | 🟡 Partial | The verified-answer distinction carries a text label ("Verified answer" + "AI · sourced") not color-only, which partially satisfies the invariant. But status/tags still lean on emoji (✓ 🛡️ 👍), vote controls are static `<span>`s (not keyboard-operable buttons), and there are no ARIA roles, focus states, live-region count announcements, or semantic OP/answer/reply hierarchy. |
| COMM-17 | ⏭️ Deferred | No offline-resilient composing (local autosave draft, retry-with-token idempotency, cross-device reconcile). No reply composer exists to attach it to; requires the offline-composing subsystem. Not implemented, no seam. |
| COMM-18 | ⏭️ Deferred | Thread statically shows a fixed "✓ Verified answer" checkmark — the exact stale-marker anti-pattern the story guards against. Needs a live ledger subscription that withdraws the marker on trust-state/source change. Seam: the ledger itself (`trust.ts` `TrustLedger`, `isVerified`) is the source of truth, but no community sync path exists. |
| COMM-19 | ⏭️ Deferred | No DSAR export or deletion/anonymization reconciliation for community posts vs. durable promotion attribution. Needs a real DB + compliance pipeline. Not implemented (no threads/votes/credits persisted to export). |
| COMM-20 | ⏭️ Deferred | No flag control, reason picker, moderator queue, flagger notification, or flag consolidation. Needs the moderation queue + persistence. Not implemented. |
| COMM-21 | ⏭️ Deferred | No event→thread / event→promotion-request conversion. Depends on the Events surface plus the (unbuilt) promotion path. Nice-to-Have; not implemented. |
| COMM-22 | ⏭️ Deferred | No thread search/filter by topic/tag/status and no full-text index. Needs a search index + access-scoped read model. Nice-to-Have; not implemented. |
| COMM-23 | ⏭️ Deferred | No graceful promotion-request failure on claim resolve/delete — depends on the promotion-request pipeline (unbuilt) evaluating against current ledger state with exactly-one terminal outcome. Not implemented. |
| COMM-24 | ⏭️ Deferred | No community moderation/integrity audit trail. Needs an append-only immutable audit store with roll-back-on-failed-log semantics. Seam: `rbac.ts` `breakGlass` produces audited records for firewall attempts (a reusable pattern), but no moderation audit log exists. |

**Counts:** 24 total — 0 ✅ Done, 11 🟡 Partial, 13 ⏭️ Deferred, 0 🚫 Out-of-scope.
