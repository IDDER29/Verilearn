### Conflicts, Trust Ledger & Sources (TRUST)

| Story | Status | Evidence / Justification |
|---|---|---|
| TRUST-01 | ✅ Done | Five/six trust states, one per claim, source-traceable, fail-closed fallback to `unsupported`, and honest roll-up aggregates all implemented in `lib/domain/trust.ts` (`TrustLedger.stateOf`, `failClosed`, `trustBreakdown`, `verifiedPercent`) + `lib/domain/types.ts` (`TrustState`, `isVerified`, `Evidence.confidence` verbatim from producer). Covered by `lib/domain/trust.test.ts`; wired to the Dashboard trust bars/breakdown via `lib/services/topics.ts` (`summarize` → `verifiedPercent`, `breakdown`, `disputes`). Never rounds up; `disputed` requires a skeptic evidence event. |
| TRUST-02 | 🟡 Partial | Faithful static Conflicts tab exists (`components/workspace/ConflictsTab.tsx`: OPEN/RESOLVED cards, dimmed resolved, "1 open conflict" count, first-time orientation panel). Per-topic dispute count IS computed from the ledger (`topics.ts` `disputes: breakdown.disputed`). Missing: no conflict entity in the store, so the tab is hardcoded mock data, not wired; count is static, not reconciled to a live list. |
| TRUST-03 | 🟡 Partial | The write mechanism exists and is tested: only a system verifier or SME-under-dual-control can set a trust state (`trust.ts` `record()`, `EpistemicFirewallError`, `DualControlError`, `smeReason`/`smeApprovedBy`), single-source-of-truth ledger object. Missing: no conflict-adjudication flow (choose competing position, draft/edit rationale, "Record resolution", decrement counter, propagate). Adjudication UI in `ConflictsTab.tsx` is static; no rationale/audit persistence path. |
| TRUST-04 | 🟡 Partial | Faithful static coverage matrix exists (`components/workspace/SourcesTab.tsx`: claims × sources grid, filled/empty/dashed cells, coverage figures). `trustBreakdown` counts unsupported. Missing: no claim×source coverage relation or coverage-health computation (`covered ÷ total`) in the engine — matrix cells and "83%" are hardcoded, not derived from a ledger-backed matrix. |
| TRUST-05 | ⏭️ Deferred | Needs the real execution sandbox to produce reproducible traces + raise computational conflicts; `DeterministicVerifier` (`lib/domain/pipeline.ts`) stands in as the adapter seam and `verified_execution` state exists. The disputed→excluded-from-tests half is already provided by `eligibleClaims` (`tests-engine.ts`). Missing until real sandbox: conflict entity, trace storage (inputs/code/observed vs expected), non-determinism handling (TRUST-18). |
| TRUST-06 | 🟡 Partial | Per-topic dispute counts are computed from the ledger and surfaced on the Dashboard (`topics.ts` `listTopicSummaries` → `disputes`; Dashboard shows conflict counts). Missing: no unified cross-topic global Conflicts inbox surface, no canonical conflict object with two-way topic↔inbox sync, no live rail-badge reconciliation. |
| TRUST-07 | ✅ Done | Eligibility provider done and tested: `eligibleClaims` filters to verified/sourced only, excluding `disputed`/`unsupported`/`interpretive` (`lib/domain/tests-engine.ts`, `isTestEligible` in `types.ts`); tests "a disputed claim can never be eligible" and "keeps verified & sourced claims, drops disputed/unsupported/interpretive" (`tests-engine.test.ts`). Eligibility computed live from ledger at pool-assembly; `predictReadiness` suppresses readiness on `disputedInScope`. |
| TRUST-08 | 🟡 Partial | SME trust-authority modeled and tested: dual-control SME write path in `trust.ts` (`record()` requires `smeReason` + distinct `smeApprovedBy`, else `DualControlError`; cannot manufacture `verified_execution` without a run — that's the citation/execution evidence split), and `sme_reviewer` role with `review:submit`/`source:promote` in `lib/domain/rbac.ts`. Missing: shared-library topic-scope enforcement, propagation to team learners, and the SME adjudication UI are not wired. |
| TRUST-09 | 🟡 Partial | `Source` entity with `addedBy`/`kind`/`preferred` exists (`types.ts`); attaching a citation yields `sourced` (never `verified`) by the ledger's evidence rules. Missing: no attach-source service/write path, no coverage recompute on add, no "Added by you" origin wiring; SourcesTab is static. |
| TRUST-10 | 🟡 Partial | `interpretive` state exists (`types.ts`) and is excluded from single-answer test pools (`isTestEligible` false for interpretive; `tests-engine.test.ts` "drops … interpretive"); severity/label distinct from `disputed` (`TRUST_SEVERITY`, `TRUST_LABEL`). Missing: the multi-position mapping adjudication flow (record ≥2 mapped positions with basis, no single winner) is not implemented/wired. |
| TRUST-11 | 🟡 Partial | `Source.preferred` flag in the model (`types.ts`); seed sets one preferred source per topic (`lib/store/seed.ts`). Missing: no set-preferred/remove write path, no single-preferred enforcement on write, no coverage recompute or removing-last-source→`unsupported` behavior wired. |
| TRUST-12 | 🟡 Partial | Promotion authority is enforced: `source:promote` is granted only to `sme_reviewer` in `rbac.ts` (contributors/moderators cannot self-promote/self-certify). Missing: the actual promotion flow (route reply → promote cited reference into Sources, attribute contributor credit + SME promoter in audit, recompute coverage) is not implemented; Community surfaces are static. |
| TRUST-13 | 🟡 Partial | The reopen lifecycle is implemented and tested for the linked Gap (`lib/domain/gap.ts` `reopenOnLapse`/`applyReview`, GAP-06 auto-reopen; seed gap with `origin: "conflict"`), and gap tests in `gap.test.ts`. Missing: conflict-object reopen itself (revert claim to `disputed`, preserve immutable resolution history, re-exclude from tests, notify downstream cert) — no conflict entity to reopen. |
| TRUST-14 | 🟡 Partial | The mutation boundary is enforced and tested: `team_learner` role holds no `trust:write` (NO human role does — `FIREWALL_PERMISSIONS`, `assertNoFirewallGrant`, `rbac.test.ts`), enforced server-side by the permission matrix, not UI-only. Missing: the read/discuss shared-topic UI, disabled mutation controls, and routing raised disputes to the SME queue are not wired. |
| TRUST-15 | 🟡 Partial | `instructor` role in `rbac.ts` has read permissions and zero trust-write (firewall-guaranteed, tested). Missing: the read-only coverage/conflicts audit surface and cohort weak-spot summary UI are not wired; assignment-scope denial not implemented. |
| TRUST-16 | 🟡 Partial | Non-color trust vocabulary exists engine-side: every state carries a glyph + text label (`TRUST_LABEL` in `types.ts`) so state is not color-only. Missing: the static coverage matrix cells (`SourcesTab.tsx` `GreenCell`/`BlueCell`/`EmptyCell`) are color-only divs, not a semantic table with programmatic row/column headers and per-cell text; keyboard-operable adjudication with managed focus not implemented. |
| TRUST-17 | 🟡 Partial | The hard invariant — nothing external mutates the ledger — is enforced: `trust:write` granted to no role including external/API/root (`rbac.ts` `FIREWALL_PERMISSIONS`, `assertNoFirewallGrant`, tested); the only write path is the verifier/SME firewall in `trust.ts`. Missing: no read-only structured export endpoint (scoped-token API returning claims/state/confidence/sources/coverage with revoked states represented). Needs an API layer. |
| TRUST-18 | 🟡 Partial | The "refuse rather than fake a pass" core is implemented and tested: fail-closed rendering downgrades any unresolved-evidence verified state to `unsupported` (`trust.ts` `failClosed`), and the source-hallucination guard rejects citation evidence with no resolved source (`record()`); pipeline fails closed to `unsupported` (`pipeline.ts`). Deferred half: real timeout/crash/non-reproducible run handling needs the real sandbox (`DeterministicVerifier` seam). |
| TRUST-19 | 🟡 Partial | Multiple defenses implemented and tested: prompt-injection sanitization (`sanitizeTopicInput`, VERIFY-19, `pipeline.ts`), Skeptic is advisory/raise-only and can never write a `verified` state (firewall in `trust.ts` + `pipeline.ts`), raise-vs-certify privilege separation (`rbac.ts`, `FIREWALL_PERMISSIONS`), audited break-glass that can never grant a firewall permission (`rbac.ts` `breakGlass`), and certificate revocation (`certificates.ts`). Missing: explicit freeze of a claim/source and a T&S operations surface. |
| TRUST-20 | 🟡 Partial | `VerificationEvent.producerVersion` supports silent-verdict-change detection (`types.ts`), and the ledger is single-source-of-truth. Missing: optimistic-concurrency (version/etag) guarded resolution writes, stale-write rejection prompt, and dual-attempt audit — no conflict write path yet, and the in-memory store has no etag mechanism. |
| TRUST-21 | 🟡 Partial | Enforced foundations: promotion requires an SME's individual `source:promote` grant (`rbac.ts`) and popularity can never fill a coverage cell or certify (no vote→trust-state path; only firewall evidence sets state). Missing: vote-ring/sockpuppet detection, discounting/freezing coordinated endorsements, and revocable/audited contributor credit are not implemented (needs community/abuse infra). |
| TRUST-22 | 🟡 Partial | A fixed, persist-nothing conflict/coverage showcase exists as static UI (`ConflictsTab.tsx`, `SourcesTab.tsx`) that writes to no ledger. Missing: it is not packaged as a guest-gated ephemeral demo — no "adjudication controls prompt sign-up" conversion gate, no showcase labeling, no guest routing on a deep-linked resolve. (Nice-to-Have priority.) |

Counts: 22 total — ✅ 2 Done · 🟡 19 Partial · ⏭️ 1 Deferred · 🚫 0 Out-of-scope.
