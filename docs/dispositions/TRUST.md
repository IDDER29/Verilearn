### Conflicts, Trust Ledger & Sources (TRUST)

| Story | Status | Evidence / Justification |
|---|---|---|
| TRUST-01 | ✅ Done | Five-state, source-traceable ledger is real and tested. `TrustState` enumerates the six variants (`lib/domain/types.ts:16`); `TrustLedger` (`lib/domain/trust.ts`) is the single source of truth, `stateOf` is **fail-closed** (unresolved verified → `unsupported`, `trust.ts:113/135`), unbacked/no-event claims default to `unsupported` (never a verified default), and a `citation` event without a resolved `sourceId` is rejected (source-hallucination guard, `trust.ts:69`). `Evidence.confidence` is stored verbatim from the producer (`types.ts:34`). Honest roll-up (`verifiedPercent`, `trustBreakdown`, `trust.ts:142/156`) is surfaced un-rounded-up in the Lecture tab and the Sources coverage-health readout. `disputed` iff an open conflict references the claim (seed uses `method:"skeptic"`, `seed.ts:20`). Covered by `trust.test.ts`. |
| TRUST-02 | ✅ Done | The per-topic Conflicts tab now renders the **full real open-dispute list**: `ConflictsTab.tsx` maps every `data.disputedClaims` entry to a selectable OPEN card (click to adjudicate that claim), the header badge shows the real `{flagged} open conflicts` count, the Skeptic panel's tested/flagged/held-up stats are computed from the real `claimCount` and dispute count, and the fabricated RESOLVED card + hardcoded Dijkstra fallback string are gone. When nothing is disputed it shows an honest "Every claim is settled" empty state. Resolution still routes through `resolveConflictAction` → `resolveConflict` (system-verifier re-verification), now guarded so an empty rationale can't be submitted. |
| TRUST-03 | ✅ Done | Adjudication is wired and persistent. `resolveConflict` (`lib/services/conflicts.ts:48`) records the learner's rationale, then the **system verifier** (not the user) emits the new verification event (firewall-respecting), moving `disputed → verified_source`/`sourced`, recomputing `verifiedPercent`, and writing to the same canonical ledger every surface reads (`conflicts.ts:62-80`). UI commits via `resolveConflictAction` + `router.refresh()` (`ConflictsTab.tsx:17-26`); empty rationale is blocked server-side (`conflicts.ts:57`); on write failure the card stays OPEN and keeps the draft. Rationale is saved into the immutable event's `evidence.detail` with actor + timestamp. Tested in `conflicts.test.ts`. **The "competing positions" example was worse than merely illustrative — it was a fixed, topic-mismatched fabrication**, found and fixed: `ConflictsTab.tsx` rendered two hardcoded cards ("Restrict to non-negative" / "Backed by CLRS §24.3" and "Keep as-is" / "Unsupported by sources") — verbatim Dijkstra content — under **every** dispute on **every** topic regardless of subject, so adjudicating an unrelated dispute (e.g. the seeded Merkle-trees hash-function claim) showed a graph-algorithm example as if it were the Skeptic's live analysis of that claim, falsely implying a specific source backed it. No per-claim candidate-positions data exists anywhere in `WorkspaceData`, so the two static cards were deleted outright rather than left as bait; the tab's real mechanism (the freeform rationale textarea, or the ⇄ map-as-interpretive path) sits directly below and is unaffected. |
| TRUST-04 | ✅ Done | The Sources tab now renders the **whole coverage matrix from real data**: `WorkspaceData.coverage` carries per-cell `state` and a `backsBySource` count, and `SourcesTab.tsx` builds the column headers from the topic's real sources, one row per real claim, and colours each cell from the ledger (green = verified-by-execution, blue = source-backed, dashed = unsupported row, empty = not backed). The source summary strip is generated from the real sources with their real kind label and "Backs N claims" count; the unsupported spotlight, coverage %, and backed/total health are all real, with an "Every claim is backed" state when coverage is complete. Coverage math is exercised by `sources.test.ts`. |
| TRUST-05 | ⏭️ Deferred | No real Execution Sandbox — the `DeterministicVerifier` adapter behind the pipeline Verifier port stands in (`lib/domain/pipeline.ts`). Sandbox-raised computational conflicts with reproducible traces (inputs/code/observed-vs-expected) are not produced; the seeded disputed claim is Skeptic-flagged, not sandbox-raised (`seed.ts:20`). Needs the real execution sandbox behind the existing Verifier seam. The downstream guarantee (disputed excluded from tests) is present via TRUST-07. |
| TRUST-06 | ✅ Done | The global conflict inbox is now built at `/conflicts` (`app/conflicts/page.tsx`), reachable from a dedicated **Conflicts** sidebar entry (`Sidebar.tsx`). `listRankedConflicts` (`lib/services/conflicts.ts`) gathers every open dispute across **all** the learner's topics and ranks them by importance — most-broken topic first (lowest verified%), then topic, then claim — grouped per topic with a High/Medium/Low-impact band. Each row opens the **same adjudication UI** via a deep link `/topics/conflicts?topic=…&claim=…`; the workspace route now reads the `claim` param and `ConflictsTab` pre-selects that dispute (`focusClaimId` threaded through `WorkspaceShell`). My Tasks' "Resolve N conflicts" now routes to the inbox. Ranking + owner-scoping covered by `conflicts.test.ts`. Remaining nuance: additional filter controls (by state/severity) and a cross-topic *inline* resolve (vs. deep-link into the topic tab) are R2 polish. |
| TRUST-07 | ✅ Done | Test-pool eligibility is live and tested. `eligibleClaims` filters to `isTestEligible` (verified/sourced only; `disputed`/`unsupported`/`interpretive` excluded — `lib/domain/tests-engine.ts:43`, `types.ts:116`), computed from **live** ledger state at pool-assembly; `buildTest` never pads with ineligible claims and discloses `reducedCoverage` ("not enough verified claims") honestly (`tests-engine.ts:98-116`). Readiness is suppressed by `disputedInScope` (`tests-engine.ts:208-225`). Resolving a conflict promotes the claim so it becomes eligible for future tests. Tests hub/detail surface the excluded-claim count. Covered by `tests-engine.test.ts` ("drops disputed/unsupported/interpretive"). |
| TRUST-08 | 🟡 Partial | The SME dual-control certify path exists at engine level: `TrustLedger.record` accepts an `isSME` actor only with a distinct second approver + reason (`trust.ts:62-67`, `DualControlError`), and an SME can never manufacture `verified_execution` without an execution event. RBAC defines `sme_reviewer` with `review:submit` + `source:promote` but **not** `trust:write` (`lib/domain/rbac.ts:150`). Missing: no shared-library/Teams data model, no SME adjudication UI, no scoped-topic server enforcement, and no propagation of SME curation to team learners — none of it is wired into the app. |
| TRUST-09 | ✅ Done | Learners can now **attach a source to an unsupported/disputed claim**: `addSourceForClaim` (`lib/services/sources.ts`) creates the `Source` (`addedBy` = the learner), then — respecting the firewall — the SYSTEM verifier records a `citation` event moving the claim to `sourced` and recomputes coverage/verifiedPercent; the learner never writes the trust state. The Sources tab surfaces an "Attach a source" form (title + optional ref) on the unsupported claim, calling `addSourceAction`, and the coverage matrix fills the new cell on refresh. Covered by `sources.test.ts` (attach backs the claim + fills a cell + bumps `backedCount`; already-backed and empty-title are rejected). Deeper Skeptic vetting of the reference is Deferred to the real LLM verifier; remove/unverifiable-flagging is TRUST-11. |
| TRUST-10 | ✅ Done | The interpretive adjudication flow is now built. `resolveAsInterpretive` (`lib/services/conflicts.ts`) takes ≥2 mapped positions (each may cite a source), rejects fewer than two non-empty stances, and — respecting the firewall — the SYSTEM verifier records an `interpretive` event (never the learner). Downstream is already correct and now reachable from the UI: `interpretive` is excluded from single-answer test pools (`isTestEligible` false, verified in this domain's tests) and carries its distinct glyph/label (`⇄`, `types.ts:112`). The Conflicts tab offers the second path — an "Or: both positions genuinely hold" block with two position inputs and a **⇄ Map as interpretive** button (`resolveInterpretiveAction`) alongside the pick-a-winner resolution, so the learner maps positions instead of forcing a winner. Guards (≥2 positions, disputed+owned claim) covered by `conflicts.test.ts`. Remaining nuance: per-position source pickers in the UI (the service accepts `sourceId` per position; the UI currently maps stances by text) and the automatic "you're forcing a single winner — consider interpretive" nudge are R2 polish. |
| TRUST-11 | ✅ Done | Both mutations are now built. `setPreferredSource` (`lib/services/sources.ts`) marks exactly one source preferred per topic (tested), and it remains the default citation during re-verification (`conflicts.ts`). `removeSource` deletes a source and **fail-closes** every claim it solely backed to `unsupported` via a SYSTEM `none`-method event, keeping the ledger and coverage matrix consistent and recomputing `verifiedPercent` (tested: the source disappears and its sole-backed claims become unsupported, with `downgraded` count returned). The Sources tab exposes a ★ Prefer toggle and a Remove action per source card (`setPreferredSourceAction`/`removeSourceAction`), with a confirm warning that names how many claims a removal will unsupport. |
| TRUST-12 | ⏭️ Deferred | Only the RBAC seam is present: `source:promote` is granted to `sme_reviewer` alone, not to contributors/mods/hosts, and never `trust:write` (`rbac.ts:150,159`). Missing everything else: Community is R2/R3 with no data model seeded, so there are no routed cited replies to promote, no promotion service, and no contributor-credit/audit trail. Needs the Community reply model plus a promotion flow behind the existing `source:promote` permission. |
| TRUST-13 | ✅ Done | Explicit conflict-reopen is now built. `reopenConflict` (`lib/services/conflicts.ts:110`) takes a **required reason**, and — respecting the firewall — the SYSTEM verifier (never the learner) records a new `disputed` event that reverts the claim's trust state, which **re-excludes it from test pools** (`isTestEligible` false) and puts it back in `listConflicts`. Append-only: the prior resolution stays in `history()`. `listResolvedConflicts` (`conflicts.ts:91`) surfaces disputed→resolved claims to the UI; the Conflicts tab renders a "Recently resolved" list with a per-claim **Reopen** button that prompts for the reason and routes through `reopenConflictAction` (`app/conflict-actions.ts:23`) + `router.refresh()`. Guards: empty reason rejected, already-open/foreign claims rejected. Covered by `conflicts.test.ts` (reopen reverts to disputed via `system:verifier`, re-enters open pool, reason required). Still Missing: downstream cert/T&S notification and trust-authority-role gating for reopen (R2 SME/Teams). |
| TRUST-14 | 🟡 Partial | The read/contribute-but-not-mutate boundary is enforced at the policy layer: `team_learner` holds `conflict:view`/`conflict:resolve_own`/`org:read` but no `trust:write` or `source:promote`, and `canWriteTrust()` returns false for **every** human role by construction (`rbac.ts:126,241,251`), with a load-time invariant sweep (`assertNoEpistemicWriteGrants`, `rbac.ts:215`). Tested in `rbac.test.ts`. Missing: no shared-library topic model, no disabled/absent-control UI for the role, and no "raised dispute routes to the SME queue" wiring. |
| TRUST-15 | 🟡 Partial | Read-only-for-instructor is enforced server-side by RBAC: `instructor` has `topic:read`/`claim:read`/`conflict:view` but no `trust:write`, `source:promote`, or resolve-others authority (`rbac.ts:137`). Missing: no instructor-facing audit UI showing the coverage matrix, unsupported count, and open/resolved conflicts for assigned topics, and no cohort weak-spot summary or assignment-scope enforcement. |
| TRUST-16 | ✅ Done | The coverage matrix is now a **semantic `<table>`** (`SourcesTab.tsx`): a `<caption>` (screen-reader) states the coverage summary, `<th scope="col">` names each source, `<th scope="row">` names each claim (with a screen-reader "(unsupported)" suffix on flagged rows), and every `<td>` carries **screen-reader text** describing the backing state in words — "verified by execution against {source}" / "backed by {source}" / "not backed by {source}" (`cellLabel`) — while the coloured cell glyph is `aria-hidden`. Trust is therefore conveyed by programmatic structure + text, never colour/shape alone; a `.vl-sr-only` utility (`globals.css`) carries the accessible text. Non-color vocabulary (glyph + label per state) and keyboard-operable adjudication remain from before. Verified via tsc/lint/build; coverage math still covered by `sources.test.ts`. Remaining nuance: a full automated axe/WCAG audit sweep across every workspace surface is tracked under A11Y. |
| TRUST-17 | ⏭️ Deferred | The hard invariant it depends on is enforced — nothing external can mutate the ledger: `trust:write` is granted to no role and is unreachable even via break-glass (`rbac.ts:106,326`, `canWriteTrust` always false). Missing the surface itself: no read-only API endpoint returning claims + trust state + confidence + backing sources + coverage summary, and no scoped-token model (scoped tokens/SSO are in the deferred infra list). Needs the API layer + token scoping built on the existing read-only firewall. |
| TRUST-18 | 🟡 Partial | The "never verified on evidence I could not produce" guarantee is implemented and tested at the ledger: `failClosed` downgrades any verified state whose evidence is unresolved (or a `verified_source` missing its `sourceId`) to `unsupported` (`trust.ts:135-139`), and the pipeline carries injection + hallucination guards. Absence of resolved evidence ⇒ never a verified state, by construction. Missing the sandbox-specific half: real timeout/crash/non-reproducible detection with an attached diagnostic and a lingering "needs verification" flag requires the actual execution sandbox (deferred; DeterministicVerifier adapter stands in). |
| TRUST-19 | 🟡 Partial | Substantial engine coverage: the Skeptic is raise-only and can never write a `verified` state (firewall, `trust.ts`/`rbac.ts`); the pipeline has prompt-injection + hallucination guards (`pipeline.ts`, tested); RBAC gives `trust_safety_lead` `cert:revoke`/`integrity:quarantine`/`user:ban` but never `trust:write` (`rbac.ts:183`); break-glass is audited and records firewall permissions as `granted:false, forbidden:true` (`breakGlass`, `rbac.ts:317-341`). Separation of raise vs certify is enforced. Missing: runtime freeze/quarantine actions against a specific claim/source/cert that halt its use in tests/certs, and a T&S detection/console UI, are not wired. |
| TRUST-20 | 🟡 Partial | A stale double-resolve is guarded: `resolveConflict` re-reads live ledger state and rejects if the claim is no longer `disputed` ("This claim is not disputed.", `conflicts.ts:56`), so a second writer cannot silently clobber the first, and badge counts are recomputed from the canonical ledger on each load (`page.tsx:21`). Missing: no explicit optimistic-concurrency version/etag on the write, no "this was just updated — review before overwriting" prompt, no distinct audit of both attempts, and no offline queue/re-validate — concurrency safety is incidental to the state check rather than an ETag contract. |
| TRUST-21 | ⏭️ Deferred | The coverage-semantics invariant holds: a matrix cell is filled **iff** a claim's verification evidence resolves to that source (`sources.ts:37-40`) — raw popularity can never fill a cell — and promotion is SME-only in RBAC (`source:promote`). Missing: vote-ring/sockpuppet detection, discounting/freezing of coordinated endorsements, and revocable/audited contributor credit — all depend on the unbuilt Community model. Needs Community + an anti-abuse layer. |
| TRUST-22 | ✅ Done | The `guest` role is read-limited in RBAC (`topic:read`/`claim:read` only, no `conflict:resolve_own`, `rbac.ts:114`), so a guest is structurally unable to mutate any real state. The ephemeral demo experience is now real: `/demo` (`app/demo/page.tsx`, public — no session cookie required, added to `proxy.ts`'s exempt set, live-verified via curl to return `200` unauthenticated) renders a coverage-style view (`components/DemoConflictPanel.tsx`) of a fixed scenario (`lib/demo/scenario.ts`) — a mix of Verified·execution/Verified·source/Sourced claims plus one genuinely **disputed** claim — computed by the **real** `TrustLedger`/`verifiedPercent` engine, not fabricated numbers. The "resolve this conflict" action (`app/demo-actions.ts`) re-verifies through the same firewall-respecting SYSTEM-verifier path as the real Conflicts flow (`resolveConflict`), just against a fresh, throwaway ledger instance created per call — **nothing persists**: a reload always restores the original disputed state, verified by construction (no `db` import anywhere in the demo module) and by a dedicated test (`scenario.test.ts`: repeated resolves never leak into a later `demoSnapshot()`). Converts to sign-up on resolution and is linked from the public `/pricing` hero. 3 new tests. |

**Counts:** 22 total — ✅ 12 Done · 🟡 6 Partial · ⏭️ 4 Deferred · 🚫 0 Out-of-scope.
