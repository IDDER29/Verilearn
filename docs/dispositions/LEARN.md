### Lecture & Active Listening (LEARN)

Disposition of the Lecture-tab / active-listening user stories against the current codebase. Primary surface is `web/components/workspace/LectureTab.tsx` (a `"use client"` component) fed by `web/lib/services/workspace.ts` → `getTopicView`. The domain engine (`web/lib/domain/trust.ts`, `types.ts`) is the source of the trust vocabulary. Core finding: the lecture reader shows **real ledger aggregates** (percent-verified, trust-state counts, claim/source counts, title, level from `WorkspaceData`), but the **inline prose, the underlined claims, the claim-detail side rail, the section strip, section progress, all five active-listening prompts, and the close gate are hardcoded Dijkstra demo content** — not derived from real ledger claim entries. There is no section model, no per-learner reading/gate-state persistence, and `GapOrigin` (`gap.ts:40`) has no `"lecture"` value, so no story that depends on section navigation, functional gates, gate persistence, or lecture-origin misconceptions is fully wired.

| Story | Status | Evidence / Justification |
|---|---|---|
| LEARN-01 | 🟡 Partial | Header title, level, `✓ {verifiedPct}% verified`, and the trust-summary line (`{verifiedCount} verified · {sourcedCount} sourced · {disputedCount} disputed`) are ledger-computed from `data.breakdown`/`verifiedPercent` (`LectureTab.tsx:69-128`). Missing: the section strip and "Section N of M / %" are hardcoded (`Section 2 of 4`, `50%`, static chips lines 133-153); and the zero-verified honest-treatment edge case is not handled — the green `✓ X% verified` chip always renders regardless of a low/zero figure. |
| LEARN-02 | 🟡 Partial | Underlined claims are individually focusable/activatable via keyboard (`role="button"`, `tabIndex={0}`, Enter/Space — `claimSpan`, lines 81-103) and color-coded by trust. But the four claims are a hardcoded `CLAIMS` map (lines 20-56), not mapped 1:1 to real ledger claim IDs; there is no anchoring layer. Only 3 trust colors are rendered (`verified`/`sourced`/`disputed`) — the engine's full six-state vocabulary (`verified_execution`, `verified_source`, `unsupported`, `interpretive` in `types.ts:16-22`) is not distinguished inline. |
| LEARN-03 | 🟡 Partial | Selecting a claim populates a "Selected claim" side-rail panel with claim text, trust badge (icon+label), source (title/desc/type icon), and confidence, with execution evidence for the verified claim and a "No source backs this → Conflicts" state for the disputed one (lines 235-265). Fully interactive — but across the four **hardcoded** demo claims only; nothing is read from real ledger claim entries. |
| LEARN-04 | 🟡 Partial | The "Section trust" segmented bar + labeled per-state counts ARE ledger-computed from `data.breakdown` (`execCount`/`backedCount`/`disputedCount`, lines 71-75, 215-232). Missing the per-section requirement: there is no section model, so the bar reflects the whole topic, does not update on section change, and only surfaces 3 of the state rows. |
| LEARN-05 | 🟡 Partial | Jump-chips render (Prereqs/§1/§2/§3, lines 149-153) but are static `<span>`s — no click handler, no navigation, no progress update; the "Section 2 of 4 / 50%" indicator is hardcoded (lines 133-140). No section unlock/lock logic exists. |
| LEARN-06 | 🟡 Partial | The close-gate UI shell exists — a permanently-disabled "Next section →" button with `cursor: not-allowed`, the lock message "Answer the prompt to unlock the next section", and a checklist "Explain in your words / Close the section" (lines 204-208, 267-282). But it is non-functional: the answer `<input>` and Submit button (lines 194-202) have no handler, submission never unlocks Next, and no state is recorded. No gate logic in services. |
| LEARN-07 | 🟡 Partial | No predict-before-reading prompt is rendered in the lecture; only a static checklist row "Predict the section ✓" (line 271) and a settings toggle (`settings/active-listening/page.tsx:56-62`). No free-text capture, no reveal-on-submit, no calibration emission. |
| LEARN-08 | 🟡 Partial | No functional mid-section pause-point check; static checklist row "Pause-point check ✓" (line 274) and a settings toggle only. No feedback anchoring and no lecture-origin misconception route — `GapOrigin` (`gap.ts:40`) is `"review" | "task" | "test" | "conflict"`, with no `"lecture"` origin to tag against. |
| LEARN-09 | 🟡 Partial | No cloze/fill-in-the-blank prompt is implemented in the reader. Only the settings toggle exists (default off — `active-listening/page.tsx:77`, `Toggle on={false}`). No term-blanking, recall check, or near-miss handling. |
| LEARN-10 | 🟡 Partial | No connection prompt is rendered in the lecture. Only the settings toggle exists (`active-listening/page.tsx:79-86`). No free-text capture or checklist advance. |
| LEARN-11 | 🟡 Partial | The Settings › Active Listening page renders all five per-type toggles with close-gate flagged "Recommended" and a Light/Balanced/Intensive frequency control (`active-listening/page.tsx:53-116`). But it is a static screen: toggles/slider are non-interactive presentational components, Save/Reset have no handlers, there is no user-preferences model, and nothing propagates to the lecture. |
| LEARN-12 | 🟡 Partial | The inline disputed callout renders with the reason ("this fails with negative edge weights") and a working "Tap Conflicts to resolve" action that routes to the real Conflicts tab via `onTab("conflicts")` (lines 163-187); the side-rail "No source backs this" state also routes to Conflicts (lines 248-260). The Conflicts tab itself is real/wired. But the callout content is hardcoded Dijkstra text, not driven by `data.disputedClaims`, and the inline flag does not reflect a resolved conflict on reload. |
| LEARN-13 | 🟡 Partial | The engine models the `interpretive` state (`types.ts:22,108-112`), but the LectureTab `Trust` type is only `"verified" | "sourced" | "disputed"` (line 8) and renders no interpretive treatment or competing-positions mapping. Engine-ready, UI-absent. |
| LEARN-14 | ⏭️ Deferred | No Skeptic hard-mode toggle, entitlement gate, or "hard mode active" indicator in the lecture. The added disputed/interpretive density must come from a Skeptic re-run upstream, which is the real LLM verifier — Deferred behind the `DeterministicVerifier` adapter seam (`lib/domain/pipeline.ts`). Needs the real LLM Skeptic plus Pro/Teams entitlement wiring before it can render. |
| LEARN-15 | 🟡 Partial | Partial a11y: claim spans are keyboard-reachable/activatable (Enter/Space, lines 88-89) and trust states carry icon+label text (not color-only) in the detail badge and inline callout (`TRUST_BADGE`, lines 58-62). Gaps: the disabled "Next" button is a plain `<button>` with `cursor:not-allowed` — no `disabled`/`aria-disabled` attribute or programmatic reason exposed (lines 205-207); underlined spans have no `aria-label`/announced trust level on focus; no `aria-live` region for gate state changes; side-rail detail is not programmatically associated with the selected span. No WCAG-AA verification. |
| LEARN-16 | 🟡 Partial | Deep-link routes exist and load real workspace data (`/topics/conflicts|sources|tasks` via `loadWorkspaceData`, `workspace.ts`), but there is no stable section/claim anchor model, so a Gap/test/review link cannot land on a specific section or claim, and no originating-context indicator is shown on arrival. Backward-nav is unrestricted only because no section locking exists at all. |
| LEARN-17 | 🟡 Partial | The showcase Dijkstra lecture content (real underlined claims, working claim→evidence side rail, a real disputed claim) exists inside `LectureTab.tsx`, but it is reachable only behind the auth gate (`web/proxy.ts` route auth) — there is no unauthenticated `/demo` route, no ephemeral no-persistence guest path, and no sign-up-on-persist prompt. Buildable now (no external blocker); just not wired. |
| LEARN-18 | 🟡 Partial | No resume/reading-progress model. The progress bar and completed-section marks are hardcoded (lines 133-140, 149-153); there is no per-learner, per-topic store of last-read section, satisfied gates, or prior gate responses (the store has OWNER/TENANT scoping but no reading-progress entity). Gate state is not persisted, so it cannot be restored. |
| LEARN-19 | 🟡 Partial | No offline capability: no service worker/PWA layer, no local persistence of prose/claims/gate inputs, no idempotent sync queue, and no "you're offline" indicator. Not blocked by a vendor — simply not implemented. |
| LEARN-20 | 🟡 Partial | The fail-closed invariant is implemented and unit-tested in the domain engine — `trust.ts` fail-closed rendering, source-hallucination guard, and `unsupported` as the default fallback (`types.ts:21`, `isVerified` gating at `types.ts:91`); percent-verified aggregates exclude non-verified claims. But the LectureTab UI renders no per-claim "evidence unavailable / retry" state or skeleton (claims are hardcoded, always resolved), so the story's UI-level fail-closed/loading behavior is not surfaced. |
| LEARN-21 | 🟡 Partial | The pipeline stage machine exists (`app/pipeline`, `lib/domain/pipeline.ts`, animated), but the LectureTab shows no per-section "verifying" treatment, no progressive reveal, and no in-place re-render on verification completion; a mid-verification or failed-section state is not represented in the reader. |
| LEARN-22 | 🟡 Partial | No anti-gaming validation on the close gate — the gate is entirely non-functional (disabled button, no submit handler; lines 194-208), so empty/whitespace/junk/min-length checks and genuine-vs-rejected signal emission do not exist. The rubric/effort logic that lives in `lib/domain/rubric.ts` is Tasks-domain grading, not wired to a close gate here. |
| LEARN-23 | 🟡 Partial | The read-only-over-ledger invariant holds (RBAC has no human `trust:write`, `lib/domain/rbac.ts`; the lecture only renders ledger state), and the store carries OWNER/TENANT scoping (`lib/store`). But no shared/curated team library is seeded, no per-seat reading/gate state is recorded separately from shared content, and the team-seat consumer reading flow is not built. |

**Summary:** 23 stories — ✅ 0 Done · 🟡 22 Partial · ⏭️ 1 Deferred · 🚫 0 Out-of-scope.
