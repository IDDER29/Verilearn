### Tasks & Rubric Assessment (TASK)

Honest engineering disposition of the Tasks & Rubric-based Assessment domain (`docs/prd/24-tasks.md`) against the actual codebase. The **grading engine** (`web/lib/domain/rubric.ts`, 40+ tests) is real and unit-tested; the **task/answer UI** (`app/topics/tasks`, `app/my-tasks`, `components/workspace/TasksTab.tsx`) is faithful static design not yet wired to that engine; **task/rubric generation, answer persistence, disputes, and eventing** are not yet built; and **semantic (LLM) grading + execution grading** are deferred behind seams.

| Story | Status | Evidence / Justification |
|---|---|---|
| TASK-01 | 🟡 Partial | Static Tasks tab exists (`components/workspace/TasksTab.tsx`: type badges, "1 of 3 tasks passed") mounted via `app/topics/tasks/page.tsx` → `WorkspaceShell`. Not wired to real data; `TaskRecord` type + empty `tasks` Map exist in the store (`lib/store/entities.ts`, `db.ts`) but nothing seeds/lists them; honest empty-state ("no gradeable tasks yet") not implemented. |
| TASK-02 | 🟡 Partial | Grading composition exists & tested: `gradeSubmission`/`hitsFor` in `rubric.ts`, "gradeSubmission composes matching + grading" test. `TaskRecord.submittedAnswer`/`submissionHits` fields exist to preserve the answer. But the answer field is static UI (not wired), and truly *semantic* (non-keyword) grading needs the real LLM verifier — the shipped `keywordMatcher` is a deterministic stand-in. |
| TASK-03 | ✅ Done | `rubric.ts` `grade()` returns per-criterion hit/missing partition with weighted `scorePct` and `passed`; `PASS_THRESHOLD = 0.75` inclusive. `rubric.test.ts` covers 74%→fail, exactly 75%→pass, 3-of-4→pass, malformed-rubric rejection (unknown criterion, missing sourceId, dup id, empty). Core scoring behavior implemented and tested (UI rendering of "Partially correct · 60%" is static, but the produced score/state drive it). |
| TASK-04 | ✅ Done | `rubric.ts` `Criterion.sourceId` is REQUIRED (criteria without a source are rejected); `assertRubricGradeable` gates every criterion to a test-eligible claim (Verified·execution/Verified·source/Sourced). `rubric.test.ts` "trust-state gating" rejects disputed/interpretive/unsupported/unresolved anchors. "How it's graded" panel in `TasksTab.tsx` is static copy, but the source-anchoring invariant is implemented and tested. |
| TASK-05 | 🟡 Partial | Revise-to-pass is tested ("a re-grade with more hits flips passed from false to true against the same rubric", `rubric.test.ts`) — `grade` is pure/re-runnable. Missing: targeted follow-up question and the 2-min micro-chapter remediation (both need the LLM generator), and the revise UI is not wired. |
| TASK-06 | 🟡 Partial | The `passed` flag on `TaskRecord`/`GradeResult` is the primitive a model-answer lock would hinge on, and the static tab references a model answer. No lock/reveal/give-up-records-unpassed logic is implemented or wired. |
| TASK-07 | 🟡 Partial | Static progress ring "1/3 passed" in `TasksTab.tsx`; `TaskRecord.passed` exists to count genuine passes. No counting/aggregation service wires the ring to real state, and topic-completion contribution is not computed. |
| TASK-08 | 🟡 Partial | `app/my-tasks/page.tsx` is a faithful static My Tasks screen (Due today/This week groups, All/To do/Revise/Done filter chips, hardcoded counts, links to `/topics/tasks`). No backend aggregation across topics; counts and urgency grouping are not computed from real task state. |
| TASK-09 | 🟡 Partial | The invariant "criteria only from Verified·execution/Verified·source/Sourced claims" is enforced in the engine (`assertRubricGradeable`, `rubric.test.ts`), and `pipeline.ts` produces the verified claim set. But no generator turns that claim set into tasks + source-derived rubrics; real generation additionally needs the LLM. `Criterion.claimId`/`sourceId` provide the data seam. |
| TASK-10 | ⏭️ Deferred | Execution-graded computational tasks need the real Execution Sandbox; only the `DeterministicVerifier` adapter behind the `Verifier` port (`pipeline.ts`) stands in. `Criterion` has no executable flag yet and no sandbox integration exists. Adapter seam present; needs the sandbox vendor/infra. |
| TASK-11 | 🟡 Partial | The grader itself refuses to grade disagreement against disputed/unsupported/interpretive claims (`assertRubricGradeable`), and `gap.ts` recognizes a `conflict` origin. But the learner-raises-dispute-on-a-criterion → Conflict-object → Skeptic/SME adjudication flow is not implemented or wired (Conflicts screen is static). |
| TASK-12 | 🟡 Partial | `rbac.ts` defines the `sme_reviewer` role (dual-control) with no `trust:write`/pass-fabrication capability, and `grade` recomputes deterministically over a corrected rubric. Missing: the adjudication surface (uphold/overturn/amend), criterion correct/retire, and cohort-wide recompute + gap-retraction wiring. |
| TASK-13 | 🟡 Partial | `tests-engine.ts` already draws from the same verified/sourced base (`eligibleClaims` verified-only, ≥75 `scoreTest`, `predictReadiness`) — the shared-scope guarantee is honored engine-side and tested. But task pass-rate is not generated or fed into predicted-readiness, and the "excluded-from-test-until-resolved" flag on conflicted claims is not surfaced. |
| TASK-14 | 🟡 Partial | `gap.ts` supports origin `"task"` with a single intake point, severity, and GAP-06 auto-reopen (`gap.test.ts`) — the Gap Map can receive and later close a task-miss. Missing: emission of task-miss/pass events from a task-grading service (none exists) and the miss→gap wiring. |
| TASK-15 | ⏭️ Deferred | Answer-preserved-and-retried grading needs a real async grading job + backend/DB for idempotent, tab-close-surviving submissions. Not built. `TaskRecord.submittedAnswer` gives the persistence seam; the idempotent server job, "grading…/retry" state, and grade-ready notification require the real grader + DB. |
| TASK-16 | 🟡 Partial | Only a static answer field exists; local draft persistence across reload/offline, offline-disabled submit, reconnect-queue, and last-writer-wins across devices are not implemented (client-side offline feature, not yet built). |
| TASK-17 | 🟡 Partial | A prompt-injection sanitizer pattern exists — `sanitizeTopicInput` in `pipeline.ts` (VERIFY-19) neutralizes injected instructions in topic input — but it is not applied to learner task answers. Copy-through/plagiarism detection against the model answer/lecture prose and pass-farming rate-limiting/anomaly detection are not built (need the real grader). |
| TASK-18 | 🟡 Partial | `signals.ts` implements the four honest signals with none/low/ok confidence and no vanity fields (`signals.test.ts`), and `rbac.ts` gives `instructor` pedagogical authority with NO `trust:write`/pass-fabrication. Missing: cohort task assignment, aggregate pass/revise/to-do + most-missed-criteria rates, and policy-bounded per-learner visibility wiring. |
| TASK-19 | 🟡 Partial | The static rubric renders hit/missing states, but non-color (icon/text/ARIA) encoding, keyboard operability of answer/submit/revise/model-answer controls, and spoken score/pass announcements are not verified, tested, or wired. |
| TASK-20 | 🟡 Partial | `rbac.ts` defines `support_agent` (consent-gated, default-deny impersonation) with audited break-glass that always records and cannot grant firewall permissions (`rbac.test.ts`) — support can never fabricate a pass. Missing: task-specific re-queue/clear-stuck-grading/restore-state actions and their per-task audit log. |
| TASK-21 | 🟡 Partial | `grade` is pure so re-grading over a reduced rubric is inherent, and `assertRubricGradeable` already refuses criteria anchored to disputed/revoked claims. Missing: the live suspend-criterion-on-claim-change flow, versioned recompute over remaining criteria, "under re-verification" degenerate-rubric handling, and learner notification — requires ledger eventing/subscription. |
| TASK-22 | 🟡 Partial | No minimum-substance/empty-answer check is implemented (no submit-gating validation, no paste-of-prompt/lecture detection, no idempotent double-submit guard). Only the static answer field exists; the substance floor and "own words" flag are unbuilt. |
| TASK-23 | 🟡 Partial | The authority model is in place: `rbac.ts` denies rubric-edit/trust authority to `learner`, the store enforces OWNER/TENANT scoping so shared data never leaks (`store.test.ts`), and `gap.ts` supports shared task-miss origin. Missing: shared-library task consumption, inherited-rubric versioning on upstream correction, and shared-gap contribution wiring. |
| TASK-24 | ⏭️ Deferred | DSAR export/deletion, per-tenant retention windows, and anonymize-not-delete for audit-critical pass records need a real database + compliance tooling. `TaskRecord` stores the learner answer (the personal-data seam) but no export/delete/retention path exists. |

**Counts:** 24 stories — ✅ 2 Done · 🟡 19 Partial · ⏭️ 3 Deferred · 🚫 0 Out-of-scope.
