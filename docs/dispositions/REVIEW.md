### Review / FSRS, Confidence & Calibration (REVIEW)

| Story | Status | Evidence / Justification |
|-------|--------|--------------------------|
| REVIEW-01 | 🟡 Partial | Wired review loop (`app/review/page.tsx`) shows "Card N of TOTAL", progress dots, and an inline "Committing first keeps you honest" explainer. Missing: a one-time, dismissible, per-account primer (seen-state), and an estimated-time indicator. |
| REVIEW-02 | ✅ Done | Commit-before-reveal is wired: "Show answer" is `disabled` until a confidence level is picked, labeled "Pick a confidence level to reveal", then "You said: {level}" is shown on the revealed card (`app/review/page.tsx`). The confidence↔outcome record is logged via `gradeCardAction`; calibration vocabulary + engine in `lib/domain/calibration.ts` (covered by `calibration.test.ts`). |
| REVIEW-03 | 🟡 Partial | Revealed card shows the real cited source (`c.source` = seeded `sourceRef`) plus a "Verified card" front marker (`app/review/page.tsx`). Missing: the badge is a fixed "Verified card" label, not derived from the claim's live trust state (Verified·execution vs Verified·source vs Sourced distinction not rendered). |
| REVIEW-04 | 🟡 Partial | FSRS-4.5 engine fully implemented and tested — `schedule`, `nextIntervals`, per-card stability/difficulty, target-retention + max-interval caps (`lib/domain/fsrs.ts`, `fsrs.test.ts`); rating advances FSRS and persists via `gradeCard` (`lib/services/review.ts`). Missing: the four interval labels in the UI are hardcoded placeholders ("2 days", "4 days", "9 days"), not computed from `nextIntervals(card)` as the AC requires. |
| REVIEW-05 | ✅ Done | Calibration engine implemented + tested: commit-vs-recall, Brier-style score, named direction (overconfident/underconfident/well-calibrated), and insufficient-data guard (`lib/domain/calibration.ts`, `calibration.test.ts`). Wired to real review data via `calibrationFor` over `reviewLog` (`lib/services/review.ts`), feeding the Calibration honest signal (`lib/domain/signals.ts`). Note: the per-card in-session verdict UI is still a static heuristic. |
| REVIEW-06 | 🟡 Partial | Blind-spot signal aggregation over drill outcomes exists and is tested (`lib/domain/signals.ts` `drills` input, `signals.test.ts`); Session Complete statically references "caught 2 seeded errors". Missing: no drill generation/seeding/mixing/flagging in the loop; controlled false-claim supply needs the Skeptic (real LLM, deferred). |
| REVIEW-07 | 🟡 Partial | Again rating auto-reopens (or opens) a Gap Map entry tagged `origin: "review"` — wired in `gradeCard` via `onLapse`/`openGap` (`lib/services/review.ts`); GAP-06 auto-reopen-on-lapse covered by `gap.test.ts`. Missing: a distinct "I had the wrong idea" action, lecture-section jump-back anchor, and micro-remediation link. |
| REVIEW-08 | 🟡 Partial | Faithful static Discuss screen exists (`app/review/discuss/page.tsx`), not wired. Sourced Skeptic pushback needs a real LLM verifier (deferred; `pipeline.ts` Verifier port is the adapter seam). No Conflict/Gap/Accept routing or prompt-injection-hardened thread yet. |
| REVIEW-09 | 🟡 Partial | Faithful static Session Complete screen exists (`app/review/complete/page.tsx`) with hardcoded figures (4 cards, 2 errors, Thursday). Not wired to actual session totals / ratings breakdown / streak / next batch. |
| REVIEW-10 | 🟡 Partial | Wired review page shows a real session ring (reviewed/total from due cards), progress dots, and "Card N of TOTAL" that update per rating (`app/review/page.tsx`). Missing: "Up next" preview list with item types, estimated time remaining, and a running blind-spot tally. |
| REVIEW-11 | 🟡 Partial | FSRS `schedule` uses caller-supplied `now`, so early reviews with true elapsed time are supported by the engine. Missing: `getDueCards` only returns cards with `fsrs.due <= at` — no "review ahead" session over not-yet-due cards, no lowest-retention prioritization; only static "Review ahead" buttons exist. |
| REVIEW-12 | 🟡 Partial | `getDueCards` already aggregates across all of a learner's topics (mixed-topic session), and the Dashboard shows real due counts. Missing: a dedicated cross-portfolio due surface with per-topic breakdown, overdue-vs-due-today distinction, and daily-limit "N more beyond today's limit". |
| REVIEW-13 | 🟡 Partial | FSRS engine honors `requestRetention` (clamped) and `maximumInterval` (`lib/domain/fsrs.ts`, tested). Settings screen (`app/settings/review/page.tsx`) is static — sliders/limits not wired, not persisted, no Save/Reset or backlog-warning; daily limit not implemented. |
| REVIEW-14 | 🟡 Partial | Static Review & FSRS settings screen exists (`app/settings/review/page.tsx`). Gate / drills / reminder toggles are not wired, not persisted, and don't affect the next session; disabling-gate calibration semantics are engine-supported (records simply not logged) but not toggle-driven. |
| REVIEW-15 | 🟡 Partial | Eligibility predicate implemented + tested: `eligibleClaims`/`isTestEligible` filter to Verified·execution/Verified·source/Sourced only, excluding disputed/unsupported/interpretive (`lib/domain/tests-engine.ts`, `tests-engine.test.ts`); trust states in `lib/domain/trust.ts`/`types.ts`. Missing: review-card generation isn't a live, trust-filtered path — cards are seeded (`lib/store/seed.ts`) rather than recomputed from current claim eligibility. |
| REVIEW-16 | 🟡 Partial | Trust-ledger primitives and downgrade propagation exist and are tested (`lib/domain/trust.ts`, certificate downgrade-propagation in `certificates.ts`). Missing: no event-driven suspension/restore of review cards on claim-state change or source revocation, no FSRS-history-preserving restore, no learner notification. |
| REVIEW-17 | ⏭️ Deferred | No offline capture/reconciliation — `gradeCardAction` persists synchronously to the in-memory store. Needs real client-side offline storage + a real database for idempotent sync/conflict-resolution. Seam: `schedule` is pure and re-applyable, which supports idempotent reconciliation once infra exists. |
| REVIEW-18 | ✅ Done | Wired review page renders an honest "All caught up!" state when `getDueCards` returns none, with next-resurface copy and a dashboard link, and never manufactures cards (`app/review/page.tsx`, `cards.length === 0` branch). |
| REVIEW-19 | 🟡 Partial | FSRS feeds true elapsed time on overdue reviews so scheduling stays correct (`lib/domain/fsrs.ts`). Missing: no daily-limit capping of overdue cards, no "clear N/day" plan framing, no spread/reschedule tool; `getDueCards` returns the full due set soonest-first. |
| REVIEW-20 | 🟡 Partial | Foundational honesty exists — `signals.ts` has no vanity fields, calibration/blind-spot are earned-by-construction, and `rbac.ts` provides audited break-glass + default-deny. Missing: no behavioral anomaly detection (juking / rapid-fire / streak-farming / blind-flagging), no signal quarantine/freeze, no appeal path. |
| REVIEW-21 | 🟡 Partial | The epistemic firewall is implemented + tested: no human role holds `trust:write`, audited break-glass, default-deny impersonation (`lib/domain/rbac.ts`, `rbac.test.ts`) — support provably cannot fabricate a signal. Missing: the scoped-consent operational restore flow (requeue session, restore streak, re-run sync) is not built. |
| REVIEW-22 | 🟡 Partial | Confidence levels and FSRS ratings carry text labels (not color-only) and are native keyboard-operable `<button>`s; the locked-reveal state has a text label ("Pick a confidence level to reveal") (`app/review/page.tsx`). Missing: ARIA live announcements for lock/reveal/interval, verified focus order/visible focus, `prefers-reduced-motion` equivalents, and a WCAG AA contrast audit. |
| REVIEW-23 | 🟡 Partial | Store models review/FSRS/calibration data per-learner (`lib/store/`); a static settings surface exists. Missing: "reset review history" Danger-zone action, DSAR export/deletion of review data, retention windows, and stricter minor defaults — needs real persistence/export infra to enforce end-to-end. |
| REVIEW-24 | 🟡 Partial | Store enforces OWNER/TENANT scoping so per-learner FSRS/calibration stays private (`lib/store/db.ts`, scoping tests). Missing: shared-library card sourcing into a seat learner's queue, inherited (unalterable) trust states, and wrong-idea contribution into a shared team Gap Map — needs the Teams/shared-library model (largely deferred). |

Counts: 3 Done, 20 Partial, 1 Deferred, 0 Out-of-scope (total 24).
