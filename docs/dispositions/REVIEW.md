### Review / FSRS, Confidence & Calibration (REVIEW)

Evidence base: pure engines `web/lib/domain/fsrs.ts` (+`fsrs.test.ts`), `web/lib/domain/calibration.ts` (+`calibration.test.ts`), `web/lib/domain/gap.ts`, `web/lib/domain/signals.ts`; services `web/lib/services/review.ts` (+`review.test.ts`), `web/lib/services/progress.ts`; server actions `web/app/review-actions.ts`; interactive UI `web/app/review/page.tsx`; static screens `web/app/review/{reveal,discuss,complete}/page.tsx`, `web/app/settings/review/page.tsx`; seed `web/lib/store/seed.ts`.

| Story | Status | Evidence / Justification |
|-------|--------|--------------------------|
| REVIEW-01 | ✅ Done | The session shows the gated first card with real position/progress dots, and a **real one-time, per-account, dismissible primer** now leads it: a `flags.reviewPrimerSeen` preference (`UserPrefs`, defaults false) backs a "How review works: commit before you reveal" banner in `web/app/review/page.tsx` that shows only until "Got it ✕" is clicked (`dismissReviewPrimerAction` → `updatePrefs`, gated by `reviewPrimerSeenAction`), so it never re-shows. The earlier hardcoded "~2 min left" is already replaced by the real remaining-count (REVIEW-10). Covered by a `prefs.test.ts` case (flag defaults false, persists dismissal, other sections untouched). |
| REVIEW-02 | ✅ Done | Commit-before-reveal is wired and live. In `web/app/review/page.tsx` the "Show answer" button is `disabled` until a confidence level is picked, labeled "Pick a confidence level to reveal the answer"; selecting a level unlocks reveal and the revealed card shows "You said: {level}". The committed level is passed through `gradeCardAction` → `gradeCard` and persisted to `reviewLog` (`web/lib/services/review.ts`). The deep-link guard is now in place: `web/app/review/reveal/page.tsx` `redirect("/review")`s, so a revealed answer is unreachable without committing a confidence in a live session. Not yet enforced: the disabled-gate (REVIEW-14) confidence-gate-off branch. |
| REVIEW-03 | ✅ Done | The card's trust badge is now derived from the claim's **live ledger state**, not a static label: `getDueCardsAction` computes `trustState = ledgerFor(topic).stateOf(claimId)` per card (`app/review-actions.ts`, REVIEW-03) and the front badge renders it via `TRUST_BADGE`, visually distinguishing **Verified · execution / Verified · source / Sourced / Interpretive / Disputed / Unsupported** (each with its own colour + label, `app/review/page.tsx`) — e.g. the seed deck shows "Verified · execution" for the sandbox-run claim and "Verified · source" for the CLRS-cited ones. Deriving at read time (rather than a persisted `ReviewCardRecord.trustState`) means the badge is always current — a claim later disputed shows Disputed on its next review. Remaining nuance: the revealed-answer **source line** still reads the card's seed `sourceRef` string rather than the claim's latest citation event, and trust-eligibility gating of which claims become cards is REVIEW-15. |
| REVIEW-04 | ✅ Done | FSRS engine complete + unit-tested (`web/lib/domain/fsrs.ts`), and the four rating buttons now show the card's **real projections**: `getDueCardsAction` (`web/app/review-actions.ts`) calls `nextIntervals(card.fsrs, now)` per due card and returns `formatInterval`-labelled Again/Hard/Good/Easy intervals, which `web/app/review/page.tsx` renders in place of the old hardcoded "< 1 min / 2 days / 4 days / 9 days". `formatInterval` (`web/lib/format.ts`) is pure and covered by `format.test.ts` (minute/hour/day/month/year scales + monotonicity); `gradeCard` still persists the actual reschedule. |
| REVIEW-05 | ✅ Done | Calibration engine done + tested (`web/lib/domain/calibration.ts`), aggregated per-learner on Progress. The in-session "Calibration check" panel (`web/app/review/page.tsx`) now derives a **real per-card verdict** from the committed confidence *and* the actual rating via `calibrationVerdict(confidence, rating)`: it names Well-calibrated / Under-confident / Over-confident by the true confidence×outcome quadrant (e.g. Confident+again → "Over-confident … this becomes a tracked gap"), and shows a prompt until a rating is chosen — no more always-"got it right". |
| REVIEW-06 | ✅ Done | The seeded error-drill mechanic is now real and live in the review session: `web/lib/services/drills.ts`'s `nextDrillFor`/`submitDrillAnswer`/`blindSpotSummary` serve a not-yet-attempted drill among the learner's own topics, record a genuine true/false judgment (fail-closed on foreign/archived/already-answered/duplicate drills), and compute a real caught/missed verdict against the drill's actual answer — never fabricated. The Review page's "Blind-spot check" card (`app/review/page.tsx`'s `BlindSpotCard`) is fully interactive: it fetches the next drill, lets the learner answer True/False, and shows the real verdict + explanation, replacing the old hardcoded "6 / 9 caught" and "none this session" copy with an honest live rate ("—" until the first attempt). The toggle to turn drills off (`Settings › Review`, REVIEW-14) is now genuinely consumed: `nextDrillFor` returns null when `prefs.review.drills === false`. A missed, claim-anchored drill now genuinely opens or reopens a Gap Map entry too (`applyDrillMiss`, origin `drill` — GAP-07), with an in-widget "🎯 Tracked as a gap" deep-link, so Gap-Map-on-miss is real and does **not** wait on the Skeptic. Covered by `lib/services/drills.test.ts` (15 tests). **Disclosed limitation:** drill *content* is hand-authored seed fixtures (3 statements, `lib/store/seed.ts`) — controlled false-claim generation/approval and the anti-blind-flag mixing rule still need the Deferred LLM Skeptic; the catch mechanic, blind-spot signal, and gap-linking it feeds are genuinely real and tested. |
| REVIEW-07 | ✅ Done | Wired + tested. `gradeCard` (`web/lib/services/review.ts`) on an `again` rating auto-reopens an existing gap via `onLapse` or opens a fresh one tagged `origin: "review"`, and `again` reschedules to a sub-day relearning step (`fsrs.ts` `RELEARN_STEP_DAYS`). The "I had the wrong idea" button in `web/app/review/page.tsx` sets rating `again` and persists through the same path. Tested by `review.test.ts` ("opens a new gap (origin review)", "auto-reopens a previously closed gap"). Not built: lecture-section jump-back anchor and micro-remediation link. |
| REVIEW-08 | ✅ Done | **Discuss is now a real, data-driven screen wired to the real engines, not a static Dijkstra example.** `review/page.tsx`'s "Discuss this answer" link carries the real card id (`?card=`); `lib/services/review.ts`'s new `getDiscussContext` resolves it to the ACTUAL claim, its live ledger trust state, and its source, scoped to the owner (null for an unknown/foreign card, surfaced as an honest "Nothing to discuss right now" empty state rather than fabricated content). The "claim you're questioning" panel and its trust badge are now real. **Raise-Conflict and Track-as-gap are genuinely wired**: `components/review/DiscussPanel.tsx` calls the same `raiseDisputeAction` the Conflicts tab uses (a required reason, then a deep-link to `/topics/conflicts?topic=&claim=` for the topic to resolve) and the same `gradeCardAction(cardId, "unsure", "again")` the live review session uses for "Track as a gap" (reschedules FSRS and opens/reopens a tracked gap, exactly like clicking "I had the wrong idea" on the review page itself would). A rejected attempt (empty reason) surfaces the genuine error, never a silent no-op. The Skeptic's reply stays a labeled sample ("AI · sample") — live sourced pushback still needs the Deferred LLM Skeptic — but that was never this story's ask; the three action choices were. **Found and fixed a severe pre-existing production bug while wiring this**: `app/review-actions.ts` (a `"use server"` file) re-exported `DueBreakdown`/`BlindSpotSummary`/`DrillPrompt` via `export type { ... }`, which this Next.js version's production bundler cannot evaluate — it threw `ReferenceError: DueBreakdown is not defined` at module-evaluation time in an actual `next start` server, which failed the ENTIRE server-action module and left the live `/review` page stuck on "Loading your review session…" forever for every real user. Fixed by having consumers import those types directly from their real source modules instead of through the re-export — verified live in a real browser (the review deck now genuinely loads, "Card 1 of 4"). 2 new tests (`getDiscussContext`: resolves the real claim/state/source, null for unknown/foreign cards). Verified end-to-end in a real browser: reached a due card, tapped "Discuss this answer," saw the SAME real claim text, raised a conflict with a reason, and landed on the Conflicts tab with the claim genuinely disputed. |
| REVIEW-09 | ✅ Done | `web/app/review/complete/page.tsx` is now an async server component driven by `sessionSummaryFor(userId, now)` (`web/lib/services/review.ts`): it derives the session as the most-recent `reviewLog` cluster (2h window) and renders real cards-reviewed, recalled count, per-rating breakdown (again/hard/good/easy bars), session calibration score (honest "—" under MIN_RECORDS), consecutive-day streak, and the soonest upcoming due card with its weekday/date and count. Covered by two new `review.test.ts` cases (session counts + next-due; session-window exclusion). Remaining nuance (which cards *this* navigation rated, vs. the whole recent window) is a REVIEW-17 session-model concern, not a blocker. |
| REVIEW-10 | ✅ Done | The session ring, "Card {n} of {TOTAL}", and progress dots are real, and the rest of the session chrome is now wired too (`web/app/review/page.tsx`): the "Up next" list renders the **actual upcoming cards** (`cards.slice(card+1, card+4)` with each card's real question, topic and section), the footer shows the **real remaining count** ("N cards left in this session"), and the card chrome shows the card's real `topicTitle` + section (`sectionLabel`) instead of a hardcoded "Dijkstra … §1". The fabricated "6 / 9 caught" blind-spot tally is now a genuine, interactive drill widget with a real live rate (REVIEW-06). Remaining (separate story): mid-session suspension decrement (REVIEW-16) — the daily-limit bound on total (REVIEW-19) is now real. |
| REVIEW-11 | ✅ Done | Review-ahead is built. `getReviewAheadCards` (`lib/services/review.ts`) returns **not-yet-due** (`fsrs.due > now`), still-eligible cards ordered by **lowest retrievability first** — the ones closest to being forgotten — using the FSRS `retrievability` model, capped at a sane limit. `reviewAheadCardsAction` maps them to the session shape (sharing `toSessionCard` with the due deck), and the caught-up screen now has a real **"Review ahead →"** button that loads them into the same session, with an honesty note that the intervals shown are the true, un-fudged next dates. Covered by `review.test.ts` (only not-yet-due, disjoint from the due deck, ascending-retrievability order, owner-scoped). |
| REVIEW-12 | ✅ Done | The mixed-topic session now has a real **per-topic breakdown**. `dueBreakdown` (`lib/services/review.ts`) groups the eligibility-gated due deck by topic (most-due first), counts **overdue** cards (>1 day past due) per topic, reads the learner's **daily limit**, and computes the **"N beyond today's limit"** overflow. The review session's "Today's queue" panel renders it: per-topic `{due}` with a red `· N overdue`, and a "N beyond today's limit of M — they'll roll into tomorrow" note. Per-card **source-topic labels** are already real in the session (card chrome + "Up next" both read the card's `topicTitle`, not a hardcoded string). Covered by `review.test.ts` (per-topic counts sum to the total, sorted, beyond-limit overflow at a small daily limit, owner-scoped). Remaining nuance: an explicit calendar "due today vs due earlier this week" split beyond the >1-day overdue flag needs learner-timezone day boundaries (shared HOME-03 nuance). |
| REVIEW-13 | ✅ Done | End-to-end per-learner FSRS tuning. The `UserPrefs.review` store (`targetRetention`, `dailyLimit`, `maxIntervalDays`) is real and **persisted** — Settings › Review's retention slider + max-interval/daily-limit steppers write through `saveReviewPrefsAction` → `updatePrefs` with a "Saved ✓" confirmation (`app/settings/review/page.tsx`). Those knobs now **feed the scheduler**: `gradeCard` calls `schedule(…, fsrsParamsFor(userId))` and the interval preview uses the same params (`fsrsParamsFor` maps `targetRetention/100` → `requestRetention` and `maxIntervalDays` → `maximumInterval`; the engine clamps to safe ranges). Covered by `review.test.ts` (prefs map correctly; **higher target retention schedules the card sooner**). Remaining nuance: the `dailyLimit` cap on session size is surfaced in copy but not yet enforced as a hard per-day quota. |
| REVIEW-14 | 🟡 Partial | The Review toggles **persist** through the real prefs store: confidence-gate, seeded-drills, and reminder toggles in `app/settings/review/page.tsx` write via `saveReviewPrefsAction` → `updatePrefs` (`UserPrefs.review.confidenceGate/drills/reminders`), backed by `prefs.test.ts`. **Two of the three toggles are now genuinely consumed**: `nextDrillFor` (`lib/services/drills.ts`) returns null once `prefs.review.drills` is off, so switching it off in Settings really stops drills from appearing in the session (covered by `drills.test.ts`); and `listNotifications` (`lib/services/notifications.ts`) now reads `prefs.review.reminders` and suppresses the review-due notification item entirely when it's off (NOTIF-04), covered by a new `notifications.test.ts` case. Remaining: the confidence-gate toggle is still not consumed — the review page always runs the gate (disabling it interacts with the REVIEW-05 calibration guarantee and needs a product call on whether an ungated session should still count toward calibration). |
| REVIEW-15 | ✅ Done | `getDueCards` now gates the deck by **live trust eligibility** (`lib/services/review.ts`, `cardClaimEligible`): a card whose underlying claim is currently `disputed`/`unsupported`/`interpretive` is held out via `isTestEligible(ledgerFor(topic).stateOf(claimId))` — the same verified/sourced bar Tests use — so review never reinforces contested content. It's **recomputed from the ledger on every call**, not a stored flag, so a claim disputed after its card was made drops out immediately and returns once resolved. The Dashboard due-count now routes through `getDueCards` too, keeping the header count consistent with the deck. Covered by `review.test.ts` (disputing a due card's claim removes it from the due list). Card *generation* is still seed-time, but eligibility is enforced at serve-time on every read, which is the stronger guarantee. |
| REVIEW-16 | ✅ Done | Corrected from a stale reading: **this is already real, just architected as live recomputation instead of a stored `suspended` flag + event listener** — deliberately, since a stored flag risks drifting from the ledger it's supposed to mirror. `cardClaimEligible` (`lib/services/review.ts`) reads the card's claim's CURRENT ledger trust state on every single call to `getDueCards`/`getReviewAheadCards`, holding out any card whose claim has gone `disputed`/`unsupported`/`interpretive` — proven by the existing REVIEW-15 test (disputing a claim makes its due card disappear within the same call). **The restore half — with FSRS history genuinely intact — is now proven too**: a new test disputes a due card's claim (it disappears from `getDueCards`), then resolves the conflict via the real `resolveConflict` engine (`lib/services/conflicts.ts`), and asserts the card reappears with the exact same `fsrs.due` timestamp it started with — there's no "restore" step to get wrong, because suspension never touches the card record at all; the card's own FSRS schedule is simply never mutated by a dispute, so resolving one just makes it visible again where it already was. This holds for ANY dispute/reopen path (`raiseDispute`, `reopenConflict`, or a direct system-verifier event, as both tests use) since eligibility is recomputed from the ledger, not from a specific caller. 1 new test in `review.test.ts` (REVIEW-16: restore with schedule untouched); REVIEW-15's existing test already covered the suspension half. |
| REVIEW-17 | 🟡 Partial | Happy-path server persistence only: `gradeCardAction` is a server action that writes synchronously (`web/app/review-actions.ts`). There is no local capture/queue of commits and ratings, no idempotent reconciliation, no offline/syncing UI, and no streak model — so nothing yet protects progress or streaks across a dropped connection. |
| REVIEW-18 | ✅ Done | The honest caught-up state is now fully wired via `caughtUpInfoAction` (`web/app/review-actions.ts`, returns deck size + soonest upcoming due): `web/app/review/page.tsx` distinguishes a **brand-new learner** (zero cards → "No review deck yet — start a topic" with a `/new-topic` CTA) from a **caught-up learner** (has cards, none due → "All caught up! Your next card is due {weekday, Mon D}" computed from the real soonest `fsrs.due`), and never manufactures cards. Remaining nice-to-have: a "Review ahead" offer (REVIEW-11, still unbuilt). |
| REVIEW-19 | ✅ Done | `getDueCards` remains the full eligibility-gated due deck (needed elsewhere for honest "N due" badges on the Dashboard and My Tasks), but the session actually **served** to the learner is now genuinely capped: a new `getSessionCards` (`lib/services/review.ts`) slices the due deck at the learner's real `prefs.review.dailyLimit`, and `getDueCardsAction` (`app/review-actions.ts`) calls it instead of the raw due deck. Most-overdue-first was already structurally true (ascending sort by `due` among already-due cards puts the longest-overdue first) and is now the ordering the cap is applied to, so the N *kept* cards are the N most in need of review, not an arbitrary N. The "N beyond today's limit… they'll roll into tomorrow" framing (`dueBreakdown`, already shown in `app/review/page.tsx`) is now an honest description of real session behavior, not just a UI label. Tested (`review.test.ts`). Missing: no reversible "spread"/redistribute action for the overflow — it simply rolls forward a day at a time, which is the correct default but not a learner-adjustable schedule. |
| REVIEW-20 | 🟡 Partial | Not implemented. No behavioral anomaly detection (calibration-juking, implausible timing, streak-farming), no signal quarantine/freeze, no appeal path, and no audit log for freezes. The 12-role RBAC matrix (`web/lib/domain/rbac.ts`) and the no-fabrication invariant provide a foundation, but the T&S subsystem itself is absent. |
| REVIEW-21 | 🟡 Partial | The firewall foundation exists — the epistemic firewall (no human role has `trust:write`, `web/lib/domain/trust.ts` + `rbac.ts`) and the fact that `reviewLog` is only ever written by `gradeCard` mean there is no fabrication path. But the support tooling itself (consent-scoped requeue, streak restore, re-run failed sync, audit trail) is not built. |
| REVIEW-22 | 🟡 Partial | The live loop uses semantic `<button>`s with text labels on both confidence levels and ratings (not color-only) and a text "Pick a confidence level to reveal the answer" lock message (`web/app/review/page.tsx`), so it is keyboard-operable and not purely color/timed. Missing: explicit ARIA live announcements of the lock/reveal/interval state, `prefers-reduced-motion` equivalents (ring animation, complete-screen confetti), and verified WCAG-AA contrast as a launch gate. |
| REVIEW-23 | 🟡 Partial | Corrected from a stale reading: `web/app/settings/danger/page.tsx` is **not** static — "Reset review history" (`resetReviewHistoryAction`, `app/danger-actions.ts`) really does wipe the learner's `reviewLog` and reset every `ReviewCardRecord.fsrs` to a fresh card, which genuinely clears FSRS state, calibration (derived from `reviewLog`), and the streak (also derived from `reviewLog`) in one real action. Now that seeded error-drills are real (REVIEW-06), the reset also clears the learner's `drillLog`, so blind-spot is honestly wiped alongside the other signals — not left stale. Still missing: no DSAR **export** of review/calibration data (only deletion exists, not download), and no retention windows. The COPPA age-gate (`web/lib/auth`) provides minor-safe defaults at signup, but review-data-specific minor handling is otherwise unbuilt. |
| REVIEW-24 | ⏭️ Deferred | Team shared-library review (Nice-to-Have, R2/R3 per roadmap). No shared-library topic model or team-seat model is seeded, so shared-source cards, private-vs-shared signal policy, and posting wrong-ideas into a shared Gap Map cannot exist yet. Needs the Teams/Org data model and shared-library sourcing; the individual FSRS/calibration/gap engines it would build on are in place. |

Counts: total 24 — ✅ Done 17, 🟡 Partial 6, ⏭️ Deferred 1, 🚫 Out-of-scope 0.
