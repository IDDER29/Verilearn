### Settings, Profile & Privacy (SETTINGS)

| Story | Status | Evidence / Justification |
|---|---|---|
| SETTINGS-01 | 🟡 Partial | `app/settings/*` + `components/SettingsNav.tsx` render the three groups (Account: Profile, Plan & billing; Learning: Verification, Active listening, Review & FSRS; Data: Privacy, Danger zone) with active-item highlight, and each sub-panel is a real route so deep-linking (`/settings/privacy`) works. Missing: role-scoped nav (no seat/frozen scoping), upgrade-nudge hiding for Pro, unknown-sub-path→home redirect, and per-panel load-failure retry. |
| SETTINGS-02 | 🟡 Partial | Faithful static profile form in `app/settings/profile/page.tsx` (photo, full/display name, language, timezone, read-only role/joined badge). No Save/Cancel wiring, no persistence, no photo upload validation, and no timezone→FSRS "due today" re-derivation. Static UI only. |
| SETTINGS-03 | 🟡 Partial | Change-email + security links surface belongs on the static Profile panel; Auth backend that would back it exists and is tested (`lib/auth/service.ts` email uniqueness + no user-enumeration, `auth.test.ts`). But Settings entry point is not wired to the Auth flow, and no SSO read-only note (SSO deferred). |
| SETTINGS-04 | 🟡 Partial | `app/settings/plan/page.tsx` shows Free plan copy ("3 active topics · standard verification") and an Upgrade-to-Pro link. Meters are hardcoded, not live from Topics/Pipeline; the Free 3-topic cap is enforced in the backend (`lib/services/topics.ts`) but the panel is not wired to it, and no at-limit warning / seat variant. |
| SETTINGS-05 | ⏭️ Deferred | Static billing surface only; a real payment method / invoice list needs a payments processor (Stripe) which is a deferred vendor. No card data by design. Seam: the Upgrade/Checkout routes exist statically to hand off to. |
| SETTINGS-06 | ⏭️ Deferred | Downgrade (Pro→Free) archive-not-delete flow and topic-retention chooser depend on the Monetization/billing lifecycle (deferred vendor) and plan-state store. Not implemented; the I1/I7 ledger-untouched invariant is structurally guaranteed by the append-only ledger firewall (`lib/domain/trust.ts`). |
| SETTINGS-07 | ⏭️ Deferred | Team-seat constrained surface (hidden billing, org-locked toggles, "provided by <Org>") needs the Teams/SSO/org-policy infra (deferred: SSO/SCIM). No role/policy-scoped rendering exists yet; RBAC roles exist in `lib/domain/rbac.ts` but no tenant policy overlay in the settings shell. |
| SETTINGS-08 | 🟡 Partial | Static Verification panel (`app/settings/page.tsx`) shows depth / interpretive / dispute / sandbox / Skeptic controls. Controls are not wired to the pipeline. The core invariant I1 ("changes apply to future lectures; no re-certify/downgrade of existing claims") IS enforced and tested: append-only ledger with human roles denied `trust:write` (`lib/domain/trust.ts`, `lib/domain/pipeline.ts`, `rbac.test.ts`). Pro-gating of Thorough/Ruthless not wired. |
| SETTINGS-09 | 🟡 Partial | Static toggles for Predict/Pause/Cloze/Connection/Close-gate + frequency in `app/settings/active-listening/page.tsx` (semantic-less div toggles). No Save/Reset persistence and no Lecture-domain consumption (Lecture domain itself not built); no "disabling gates → reduced-confidence" propagation to Reports. |
| SETTINGS-10 | 🟡 Partial | Static Review & FSRS panel (`app/settings/review/page.tsx`) with target-retention/daily-limit/max-interval steppers + toggles. FSRS engine exists and is tested (`lib/domain/fsrs.ts`), but the panel is not wired to it — no persistence, no on-save recompute trigger, no bounds enforcement. |
| SETTINGS-11 | 🟡 Partial | `app/settings/privacy/page.tsx` renders the honest stored-items list ("Your topics, lectures & the claim ledger" …) and the binding never-clause ("We never sell your data or train public models on it"). It is static copy: no policy versioning, no full-policy link, no material-change surfacing, and no automated sync with export contents (I4/I5). |
| SETTINGS-12 | 🟡 Partial | Static toggles present in the Privacy panel (Anonymous usage analytics, Show my profile in Community, Product emails). No persistence and — critically — no collection-layer opt-out enforcement or Community attribution wiring; toggles are non-semantic and don't reconcile to server truth. |
| SETTINGS-13 | ⏭️ Deferred | No export feature implemented (no UI, no service). Needs an async/chunked generation pipeline + time-limited authenticated download link + rate limiting. Seam: the faithful data model (claim ledger with trust states/sources/coverage) already lives in `lib/store/` and `lib/domain/trust.ts`, so a faithful JSON serialization is buildable on top. |
| SETTINGS-14 | ⏭️ Deferred | DSAR access/erasure + retention schedules need a real database, retention infra, and legal-hold tooling (deferred). Seam exists: `compliance_dpo` role with `compliance:export` / `dpo:manage` / `audit:read` permissions is defined and tested (`lib/domain/rbac.ts`, `rbac.test.ts`); the DPO-never-trust firewall is enforced (no human `trust:write`). |
| SETTINGS-15 | 🟡 Partial | COPPA age gate is implemented and tested: `lib/auth/age.ts` blocks under-13 pending verifiable parental consent and applies minor-safe defaults (13–17), coarse age-band only (`auth.test.ts`). Missing (Future): verifiable guardian-consent flow, linked guardian/dependent accounts, and the documented single-shared-account disclosure surface. |
| SETTINGS-16 | 🟡 Partial | Static "Reset review history" card in `app/settings/danger/page.tsx` with correct blast-radius copy ("Your topics & lectures stay"). No confirmation modal, no wiring to clear FSRS/calibration data, no Support-undo. The FSRS/review data it would clear exists in the store. |
| SETTINGS-17 | 🟡 Partial | Static "Reset gap map" card in the Danger zone with blast-radius copy. Not wired to the gap engine (`lib/domain/gap.ts` exists and is tested); no confirmation, no deletion, no atomicity handling. |
| SETTINGS-18 | 🟡 Partial | Static "Delete all topics" card with irreversibility copy. No cascade logic, no confirmation flow, no certificate-tombstoning (I7). Static UI only. |
| SETTINGS-19 | 🟡 Partial | Static delete-account card with type-DELETE confirmation input and "permanent / cannot be undone" copy (`app/settings/danger/page.tsx`, button rendered disabled). Missing: grace/undo window, cascade + anonymization, sole-admin transfer guard, running-pipeline cancel, and certificate verification-record retention per I7. |
| SETTINGS-20 | 🟡 Partial | Save/Reset button affordances appear in the Learning panels, but the reliable-save behavior is unimplemented: no server actions, no explicit success/failure toast, no rollback, no version/etag concurrency check, no dirty-state navigation guard, no offline banner. The in-memory repo (`lib/store/db.ts`) could back a preferences store but none is wired. |
| SETTINGS-21 | 🟡 Partial | The governance firewall (the story's critical core) is implemented and tested: `support_agent` scoped to read-only (`topic:read`,`claim:read`,`support:read_tickets`), impersonation is default-deny break-glass and audited, and no human role holds `trust:write` (`lib/domain/rbac.ts`, `rbac.test.ts`). Missing: scoped-consent-token restore/unstick tooling and before/after audit-log write path. |
| SETTINGS-22 | 🟡 Partial | Cert revocation is implemented and tested — `lib/domain/certificates.ts` revoke + fail-closed verify + downgrade-propagation; `trust_safety_lead` role/permissions exist (`rbac.ts`, `rbac.test.ts`). Missing: the account-freeze state that makes settings read-only and blocks self-deletion/export, the frozen-user honest-status/appeal surface, and grace-window pause. |
| SETTINGS-23 | 🟡 Partial | Static settings controls exist but are non-semantic: toggles/sliders are styled `div`/`span` elements (e.g. `Toggle` in `app/settings/active-listening/page.tsx`), not real inputs with roles/labels. No keyboard operability, no live-region announcements, no non-color state; WCAG 2.2 AA not yet met. The type-DELETE confirm is a plain input (reachable) but the danger actions are unwired. |

Counts: 23 total — ✅ 0 Done · 🟡 18 Partial · ⏭️ 5 Deferred · 🚫 0 Out-of-scope.
