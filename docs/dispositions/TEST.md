### Tests, Certificates & Verification (TEST)

Honest, evidence-based disposition of the TEST domain (`docs/prd/28-tests-certs.md`) against the current codebase. The pure engines (`web/lib/domain/tests-engine.ts`, `web/lib/domain/certificates.ts`) and their tests are strong; the service layer (`web/lib/services/tests.ts`, `web/lib/services/testsession.ts`) wires eligibility and real fail-closed certificate issuance. But the timed MCQ **runner is static** (explicitly Deferred — the deterministic verifier produces no MCQ options), there is **no public verify route**, and misses/gaps/readiness/retake are not yet driven by the engines end-to-end.

| Story | Status | Evidence / Justification |
|---|---|---|
| TEST-01 | ✅ Done | The tested `predictReadiness` engine (`tests-engine.ts:206`) is now **wired to real data** through a new service wrapper `readinessFor(userId, topicId, now)` (`web/lib/services/progress.ts`): it assembles the learner's real signals for the topic — retention (share of the topic's eligible-claim reviews recalled), calibration score, covered eligible claims off the ledger, how many have review history, and disputed-in-scope — and delegates the forecast to the pure engine. Test Detail (`app/tests/[slug]/page.tsx`) and the hub hero (`app/tests/page.tsx`) both render the real forecast: a dynamic readiness ring (degrees + threshold colour vs. the pass bar), an honest "—/no prediction yet" state when unreviewed, an explicit "low confidence" state under `MIN_READINESS_RECORDS`, and a basis line ("N of M covered claims reviewed"). Four new `progress.test.ts` cases cover the no-history, predicted, and topic-scoping paths. Remaining nice-to-have: a live recompute badge after acting on a lever (the value already recomputes on each server render). |
| TEST-02 | 🟡 Partial | The spine rule is real and tested: `eligibleClaims`/`buildTest` (`tests-engine.ts:43,96`) draw ONLY from Verified·execution/Verified·source/Sourced via `isTestEligible`; `buildSession` (`testsession.ts:24`) builds from a topic's eligible claims and Test Detail shows the real question count and "drawn only from verified & sourced claims" copy. Missing: the runner (`app/tests/runner/page.tsx`) is fully static (no live countdown, MCQ options hard-coded), and per-question claim+source reveal after finishing is static (`results/page.tsx` misses are hard-coded). |
| TEST-03 | ✅ Done | Real excluded/disputed count flows to Test Detail with "excluded until resolved" copy, and the **"What this test covers" section list is now real**: `[slug]/page.tsx` reads `getTopicView(user.id, topicId).claimStates`, groups them by `sectionId`, and renders one row per real section with its test-eligible claim count (`isTestEligible`) and a "needs review · N excluded" flag when a section holds disputed/unsupported claims. Because it recomputes from the ledger on each render, resolving a conflict (via `/topics/conflicts` → `resolveConflict`, the deep-linked "Boost your odds" lever) genuinely raises a section's eligible count and lowers its excluded count on the next load. Remaining nice-to-have: human-readable section titles (only `sectionId` order is known without lecture generation). |
| TEST-04 | 🟡 Partial | The runner screen (`app/tests/runner/page.tsx`) shows the intended shape — "Question 4 of 12", segmented bar, Flag/Previous/Next, "navigation is paused so the timer stays honest", "sources & explanations appear after you finish" — but it is **entirely static markup**: Next is a `<Link>` to results, Flag/Prev are inert buttons, the timer reads a fixed "12:47", and the global nav is not actually disabled. No confidence gate (correctly absent). |
| TEST-05 | 🟡 Partial | Results (`app/tests/results/page.tsx`) shows a **real** overall score + pass/fail against the bar via `submitTest` → `scoreTest` (`testsession.ts:51`), and `scoreTest` distinguishes skipped from wrong (`tests-engine.ts:126`). But the per-section breakdown, the two "missed" questions with claim + "Verified · CLRS §24.3" citations, and "nudged retention/transfer" are all hard-coded static content, not per-answer data. |
| TEST-06 | 🟡 Partial | The Gap engine (`domain/gap.ts`, Open→Watching→Closed→Reopened + auto-reopen) exists and is wired to Review, but **`submitTest` does not write any gaps** (`testsession.ts:51-70` only scores and issues a cert). The "Added to gap map" badge and "Review your 2 gaps" link on Results are static. No create/reopen-on-miss, no origin=test tagging, no retry-on-failure behavior. |
| TEST-07 | 🟡 Partial | Retake screen (`app/tests/retake/page.tsx`) renders the full concept — "attempt 2 of 3", 70% bar, "since your last attempt" deltas, "only your best score counts" — but is **entirely static**. No attempt model, no ceiling enforcement, no best-of-N recording, and `buildTest` has no weak-section weighting or fresh-vs-reused-item logic. |
| TEST-08 | 🟡 Partial | The hub (`app/tests/page.tsx`) uses one real topic for the hero (`listTestableTopics(user.id)[0]`, real eligible/excluded counts), but the entire scheduled list (Merkle trees / Binary search) and past-results section (Hashing / Graph traversal) are hard-coded, readiness bars are static, and there is no urgency sort, per-test due dates, reminders, or empty-state wiring. |
| TEST-09 | 🟡 Partial | Test Detail "Boost your odds" shows two static levers (Review §3 flashcards → `/review`, Resolve 1 conflict → `/topics/conflicts`). They point at genuine surfaces but are not ranked by predicted-readiness lift, don't update after acting, and there's no "you're ready" state. Nice-to-Have priority. |
| TEST-10 | 🟡 Partial | Issuance is real and wired: on a pass `submitTest` calls `issueCertificate` and **persists** a `CertificateRecord` to `db.certificates` with a tamper-evident verify code (`testsession.ts:60-68`); `issueCertificate` throws on a non-passing result (`certificates.ts:74`) and is unit-tested. Results shows the minted code. Missing: Mastery-vs-Checkpoint distinction (any pass mints one), full cert metadata (scope/date/bar/attestation/attempt), and view/download/share from profile + QR. |
| TEST-11 | 🟡 Partial | `verifyCertificate` (`certificates.ts:108`) is fail-closed and unit-tested (unknown→invalid, revoked→invalid, live→valid; never throws, never defaults to valid). But there is **no public, unauthenticated verify route** — `find app` shows no `verify`/`credential` page; Results merely prints the code as text. No QR, no issued-to/scope/status render, no PII-minimization surface. |
| TEST-12 | 🟡 Partial | No consent/preferences model exists (Settings privacy panels are themselves unwired). The learner cannot choose a display name/identifier on the cert, regenerate/revoke a share link, or opt into deeper-signal sharing; the `CertificateRecord` carries no learner-chosen PII fields. Not vendor-blocked, so Partial rather than Deferred, but effectively unbuilt. |
| TEST-13 | 🟡 Partial | Primitives + firewall exist and are tested: `revokeCertificate` and `shouldRevokeOnClaimDowngrade` (`certificates.ts:130,146`), and the trust ledger firewall (`domain/trust.ts` + `rbac.ts`) guarantees only a system verifier / SME-dual-control can change a trust state (no human role has `trust:write`). Missing wiring: no claim-downgrade → cert-revocation propagation, `shouldRevokeOnClaimDowngrade` does **no materiality check** (just `downgraded && !revoked`), no unstarted-test regeneration flag, no `cert.revoked` event, no "past result annotated" path. |
| TEST-14 | ⏭️ Deferred | The negative invariant is enforced+tested — the 12-role RBAC matrix gives Instructor no `trust:write` and no score-override capability (`domain/rbac.ts`). But the positive feature (assign a shared-library test to a cohort, read pass-rate/readiness distributions) needs a Teams cohort data model that is not seeded (Teams is roadmap-phased, like Community/Events). Seam: RBAC roles present. |
| TEST-15 | 🟡 Partial | `revokeCertificate` (with reason category + timestamp) exists and is tested, and RBAC bars T&S from hand-certifying. Missing: a distinct **freeze/suspend** state (the record has only `revoked`), any T&S action UI, fraud/answer-harvesting/collusion pattern detection, and the standardized reason-category + reversible audit trail surface. |
| TEST-16 | ⏭️ Deferred | COPPA age-gate exists upstream in auth (under-13 blocked pending consent) and issuance stamps `issuedAt`. But an immutable, exportable issuance/revocation audit trail, DSAR inclusion of certs, and retention-cascade to certs + verify pages need a real database and audit infra (persistence is in-memory). Future priority. Seams: COPPA gate + `issuedAt`. |
| TEST-17 | ⏭️ Deferred | No programmatic verify endpoint and no `test.passed`/`cert.issued`/`cert.revoked` webhooks exist; explicitly forward-looking per the PRD persona. `verifyCertificate` is the clean logic seam a future endpoint would call, but the API surface, webhook delivery, idempotency/ordering, and consent-scoped ledger payload are all unbuilt. |
| TEST-18 | 🟡 Partial | No accessibility affordances are present in the tests UI (grep for `aria-`/`prefers-reduced-motion`/accommodation in `app/tests` finds nothing). The runner encodes timer/progress/flag via color only, there is no extended-time accommodation model, no ARIA/text timer announcements, and no reduced-motion path. MVP priority; substantially unbuilt. |
| TEST-19 | 🟡 Partial | The server-authoritative clock primitive exists and is tested: `isExpired(startedAt, durationMs, now)` (`tests-engine.ts:249`) decides expiry purely from server timestamps, and `BuiltTest.startedAt` is server-supplied. Missing everything around it: no persisted session, no continuous answer persistence/resume, no single-session attempt lock, no reconnect reconciliation, no consumed-attempt accounting — the runner is stateless static markup. |
| TEST-20 | 🟡 Partial | The engine handles the shortfall honestly: `buildTest` never pads with ineligible claims and sets `reducedCoverage` when eligible < requested (`tests-engine.ts:112-118`); `buildSession` returns null and Test Detail degrades gracefully ("Your first test", 0 questions). Missing: an explicit "not startable — resolve these" block, routing to the exact blocking Conflicts/Sources/Pipeline items, struck-through blocked sections, and auto-becomes-startable behavior. `reducedCoverage` is computed but not surfaced. |
| TEST-21 | 🟡 Partial | `verifyCertificate` returns unambiguous negatives for unknown/forged (`reason: "unknown"`) and revoked (`reason: "revoked"` with detail) and is unit-tested to never default to valid (`certificates.test.ts`). But with no public verify page (see TEST-11) the negative-path UI, the distinct expired/retention-deleted state, tamper-evidence over a real artifact, and the "unable to verify right now" degraded state are unexercised. |
| TEST-22 | 🟡 Partial | Scoring primitives for expiry are done+tested: `scoreTest` treats unanswered as incorrect, distinguishes skips, and a tie at the bar passes (`tests-engine.ts:148-162`); `isExpired` gives the server boundary. But the runner has no live timer, no input-lock-and-auto-submit at 0:00, no "time's up — submitted" state, and no attempt-consumed flow — the end-state is a manual `<Link>` to Results. |
| TEST-23 | 🟡 Partial | The absolute firewall is enforced+tested — Support (like all human roles) has no `trust:write` in RBAC, and a certificate can only arise from a passing `scoreTest` (`issueCertificate` throws otherwise), so Support cannot fabricate a score/pass/cert. But there is no attempt/session model to reinstate or unstick, no consent-scoped support action, and no audit log — the reinstate/clear-frozen recovery flows are unbuilt. |

**Counts:** total 23 — ✅ Done 2 · 🟡 Partial 18 · ⏭️ Deferred 3 · 🚫 Out-of-scope 0.
