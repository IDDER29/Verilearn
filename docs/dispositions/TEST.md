### Tests, Certificates & Verification (TEST)

Disposition of PRD domain 28 against the actual codebase. Key finding: the domain **engine** (`web/lib/domain/tests-engine.ts`, `certificates.ts`, plus supporting `gap.ts`, `rbac.ts`, `signals.ts`) is pure and thoroughly unit-tested, but **none of the `/tests/*` app routes are wired to it** — they are faithful static screens. `grep` for `tests-engine`/`certificates`/`buildTest`/`issueCertificate`/`predictReadiness` across `app/` and `lib/services/` returns **zero** non-test usages. There is no public verify route, no attempt/session store, and no webhook/audit/eventing infra. Hence engine-backed stories are Partial (logic done + tested, UI/persistence not wired) and pure-infra stories are Deferred.

| Story | Status | Evidence / Justification |
|---|---|---|
| TEST-01 | 🟡 Partial | `predictReadiness` done + tested (`tests-engine.ts:206`; `tests-engine.test.ts` "predictReadiness (TEST-01)"): honest low-confidence "not enough data yet", disputed-in-scope suppression, unreviewed→low. **Missing:** not displayed on Tests hub / Detail / Retake (all static), no recompute-on-input-change wiring. |
| TEST-02 | 🟡 Partial | The spine rule is done + tested: `eligibleClaims` filters to Verified·execution/Verified·source/Sourced only, `buildTest` binds each question to a claim id + `sourceId` deterministically (`tests-engine.ts:43,96`; test "eligibleClaims (TEST-02 spine rule)" / "buildTest"). **Missing:** timed runner (question count, segmented progress, live countdown) is static UI (`app/tests/runner/page.tsx`), not wired. |
| TEST-03 | 🟡 Partial | Exclusion logic done + tested (disputed/unsupported/interpretive filtered by `isTestEligible`). **Missing:** "needs review" section marking, blocking-claim counts, resolve-to-re-enable, and "boost your odds → resolve 1 conflict" affordance are static UI only, not wired to the ledger. |
| TEST-04 | 🟡 Partial | Static runner screen exists (`app/tests/runner/page.tsx`) with the "navigation is paused" notice. **Missing:** Previous/Next, Flag toggle, answered/flagged progress, and disabled-nav enforcement are not interactive/wired; no backend state. |
| TEST-05 | 🟡 Partial | `scoreTest` (pct, pass/fail, skipped distinguished) done + tested; `buildTest` carries per-question claim + `sourceId` for corrections; `signals.ts` has retention/transfer/calibration. **Missing:** results screen (`app/tests/results/page.tsx`) is static — per-section breakdown, per-miss claim+citation, and "signals moved" are not wired to real scoring. |
| TEST-06 | 🟡 Partial | Gap engine supports origin `"test"` (`gap.ts:40`) and AUTO-REOPEN on lapse (GAP-06, `gap.test.ts`). **Missing:** no test-submission → Gap-Map write wiring (tests aren't wired at all); no "Added to gap map" flow, retry-on-failure, or deep-link. |
| TEST-07 | 🟡 Partial | Static retake screen exists (`app/tests/retake/page.tsx`). **Missing:** attempt-ceiling enforcement, best-of-N aggregation, weighted-toward-weak-section regeneration, "since last attempt" delta, and reuse-disclosure are not implemented (no attempt store). |
| TEST-08 | 🟡 Partial | Static Tests hub exists (`app/tests/page.tsx`) with next-test hero and scheduled/past sections. **Missing:** not wired to real topics/tests/readiness; no urgency sort, per-test readiness bars, Free-cap filtering, or empty state driven by data. |
| TEST-09 | 🟡 Partial | Static "boost your odds" affordance appears in the Detail UI. **Missing:** no recommendation engine ranking levers by predicted readiness lift; no post-action re-ranking or "you're ready" computed state. (Nice-to-Have.) |
| TEST-10 | 🟡 Partial | `issueCertificate` is pass-gated and done + tested — throws `CertificateIssuanceError` on a failing result, never revoked on issue (`certificates.ts:74`; test "issueCertificate (TEST-10)"). **Missing:** not wired to a real Mastery pass; no cert view/download/share UI, no verify-URL/QR generation, no persistence. |
| TEST-11 | 🟡 Partial | `verifyCertificate` is fail-closed and done + tested — undefined/forged → `unknown`, revoked → `revoked`, never defaults to valid (`certificates.ts:108`; test "verifyCertificate — fail-closed"). **Missing:** no public no-login verify route/page exists; PII-minimization not wired. |
| TEST-12 | 🟡 Partial | Static Settings screens exist. **Missing:** no cert-specific PII/display-name control, no share-link regenerate/revoke, no opt-in-to-deeper-signals consent logic; `Certificate` interface stores no chosen identifier or consent scope. |
| TEST-13 | 🟡 Partial | Revocation invariant partly realized + tested: `revokeCertificate` (records reason + timestamp) and `shouldRevokeOnClaimDowngrade` (revoke iff downgraded AND not already revoked) done + tested (`certificates.ts:130,146`; test "shouldRevokeOnClaimDowngrade (TEST-13)" incl. downgrade-propagation); RBAC firewall ensures only SME-side sets trust (`trust:write` granted to no human role, `rbac.ts:106`). **Missing:** materiality check (score-would-still-pass) is not modeled (`downgraded` is a bare boolean), no unstarted-test regeneration flagging, no `cert.revoked` webhook, no past-result annotation wiring. |
| TEST-14 | 🟡 Partial | RBAC authority-limits done + tested: `instructor` permission set excludes `trust:write`, `cert:revoke`, and any score override (`rbac.ts` instructor block; `rbac.test.ts`). **Missing:** cohort assignment, pass-rate/readiness distribution views, and shared-library plumbing not built (Teams). |
| TEST-15 | 🟡 Partial | `cert:revoke` granted only to `trust_safety_lead`, who has NO `trust:write` (can revoke, never hand-certify) — done + tested (`rbac.ts:180`); `revokeCertificate` engine done + tested. **Missing:** freeze/suspend distinct from revoke, pattern detection (answer-harvesting/collusion/proctor anomalies), and public-page/webhook reflection not implemented. |
| TEST-16 | ⏭️ Deferred | Needs persistent audit-log + DB + retention/DSAR infra. Seams exist: `compliance_dpo` holds `compliance:export`/`audit:read` (`rbac.ts`), and COPPA age-gate exists (`lib/auth/age.ts`). No cert issuance/revocation audit trail, DSAR export, retention cascade, or minor-consent gating on issuance is built. (Future.) |
| TEST-17 | ⏭️ Deferred | Needs external API/eventing infra. `verifyCertificate` is the source-of-truth seam, but there is no verify HTTP endpoint and no `test.passed`/`cert.issued`/`cert.revoked` webhooks (idempotent/ordered delivery, reconciliation). (Future, explicitly forward-looking.) |
| TEST-18 | 🟡 Partial | Server-clock seam supports extended time via `isExpired(startedAt, durationMs, now)` (a longer `durationMs` accommodates without marking the result). **Missing:** no accommodation config/application, no ARIA/text timer-progress-flag encodings, no periodic time announcements, no `prefers-reduced-motion` alternatives; runner is static. |
| TEST-19 | 🟡 Partial | Server-authoritative clock done + tested: `isExpired` decides purely from server timestamps, cannot be reset by reload/tab-close (`tests-engine.ts:249`; test "isExpired (server-authoritative clock, TEST-19/22)"). **Missing:** continuous answer persistence, reconnect-resume, single-session attempt lock, and clean outage invalidation — no attempt/session store exists. |
| TEST-20 | 🟡 Partial | Honest-failure disclosure done + tested: `buildTest` never pads with ineligible claims, returns fewer questions with `reducedCoverage` + `eligibleCount` when the pool is short (`tests-engine.ts:96`; test "buildTest"). **Missing:** not-startable state, "resolve X to unlock" routing to Conflicts/Sources/Pipeline, mid-pipeline "available after verification", and struck-through blocked sections are static UI only. |
| TEST-21 | 🟡 Partial | Fail-closed negative done + tested for the core cases: unknown/forged → "not a valid VeriLearn certificate", revoked → "revoked" with reason (`certificates.ts:108`; test "verifyCertificate — fail-closed (TEST-11 / TEST-21)"). **Missing:** distinct expired/retention-deleted state, degraded-service → "unable to verify" (fail to unverified), the public page itself, and cryptographic tamper-evidence (`verifyCode` is a plain caller-supplied code, not a signature). |
| TEST-22 | 🟡 Partial | Deterministic expiry + scoring done + tested: `isExpired` (inclusive boundary, server clock) and `scoreTest` (unanswered scored incorrect, skips distinguished, tie-at-bar passes) (`tests-engine.ts:148,249`; tests "scoreTest (pass bar boundary)" / "isExpired"). **Missing:** runner input-lock + auto-submit UI, "time's up — submitted" state, and attempt/gap/signal flow-through are not wired. |
| TEST-23 | 🟡 Partial | RBAC firewall done + tested: `support_agent` holds only `support:read_tickets` (read) — no score-set, no cert issue/revoke, no `trust:write`; impersonation is break-glass, granted to no role by default (`rbac.ts:168`, `rbac.test.ts`). **Missing:** attempt reinstatement and frozen-session unstick operations are not implemented (no attempt store to reinstate). |

**Summary:** 23 stories — ✅ 0 Done · 🟡 21 Partial · ⏭️ 2 Deferred · 🚫 0 Out-of-scope. The verification/eligibility/scoring/clock/revocation invariants exist as pure, unit-tested engine code; nothing in this domain is yet wired end-to-end to the app (all `/tests/*` routes are static), so no story reaches Done.
