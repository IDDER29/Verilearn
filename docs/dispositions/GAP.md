### Gap Map & Misconception Tracking (GAP)

| Story | Status | Evidence / Justification |
|---|---|---|
| GAP-01 | 🟡 Partial | Engine `openGap` seeds an `open` gap with origin/claim/topic (`lib/domain/gap.ts`, `gap.test.ts` "openGap intake"), and `gradeCard` auto-opens a `review`-origin gap on a lapse into the real store (`lib/services/review.ts:51-53`). Missing: the explicit `＋ Add to gap map` button (the review wrong-idea button links to static `/review/discuss`), the inline "Tracking this gap" confirmation, and board visibility — no board exists yet. |
| GAP-02 | 🟡 Partial | Store query `gapsOf(db, userId)` (`lib/store/db.ts:63`) and the `Gap` record model exist and are tenant-scoped/tested. Missing: no board route/UI at all (no `/gaps`), so Open/Watching/Closed columns, `Group by` Status/Topic/Severity, per-column counts, and header roll-up are not built. |
| GAP-03 | 🟡 Partial | Engine backs the detail data: status, `severity`, append-only `history` with origin, manual status control via `toWatching`/`closeGap` (`lib/domain/gap.ts`). Missing: no detail view/route, no status control UI, no rendered origin trail. |
| GAP-04 | 🟡 Partial | Revisit anchor data is stored on the gap (`claimId` + `topicId`, opaque links; `lib/domain/gap.ts:74-83`). Missing: no `Revisit` deep-link action, no claim highlight, no task/test routing, no "section updated" handling — pure UI, not built. |
| GAP-05 | 🟡 Partial | Full evidence-driven lifecycle implemented and tested: `toWatching`, `closeGap` (requires `MASTERY_THRESHOLD` sustained reviews, throws `GapEvidenceError` on fiat close), never auto-closes from `open` (`gap.test.ts` "full lifecycle", "closeGap requires evidence"). Missing: forward auto-advance (Open→Watching→Closed on correct recalls) is NOT wired into `gradeCard` — only the reopen/open-on-lapse path is wired. Thresholds are engine-only. |
| GAP-06 | ✅ Done | The headline mechanic. `onLapse` / `applyReviewOutcome` auto-reopen a closed/watching gap on a lapse, escalate severity, append (never overwrite) history (`lib/domain/gap.ts:252-286`; `gap.test.ts` "onLapse auto-reopen — THE headline mechanic"). Wired to real data: `gradeCard` calls `onLapse` and returns `gapReopened` (`lib/services/review.ts:44-49`); end-to-end covered in `store.test.ts` "a closed gap auto-reopens on a later lapse (through stored data)". |
| GAP-07 | 🟡 Partial | Origin vocabulary `review \| task \| test \| conflict` modeled; single intake `openGap` for all origins (`gap.ts:40,187-213`); seed creates a `conflict`-origin gap. Basic dedupe on the review path: `gradeCard` finds an existing gap by `claimId` before creating (`review.ts:45`). Missing: auto-creation from tasks/tests/disputes not wired (those screens are static); no normalized-misconception dedupe key, no multi-origin merge, no rate-grouping. |
| GAP-08 | 🟡 Partial | `gapsOf` can derive the zero-gaps condition, but there is no board and therefore no teaching empty state, no origin explainer, and no "no gaps match this filter" variant. Pure UI, not started; no external blocker. |
| GAP-09 | 🟡 Partial | `closeGap` implements a provisional, evidence-gated close that remains subject to auto-reopen (`gap.ts:235-250`, tested). Missing: `＋ Add a drill` (hand-off to Retain) not implemented; a by-hand "Closed · manual" action from the detail is not wired; no UI. |
| GAP-10 | ⏭️ Deferred | Discussion thread with the Skeptic-as-partner needs a real LLM verifier/chat (DeterministicVerifier stands in for verification only; no conversational Skeptic). No thread data model on the gap and `/review/discuss` is a static screen. Needs: real LLM + Pro-gating; thread persistence model. |
| GAP-11 | 🟡 Partial | Severity bands (`low/med/high`) and reopen-driven escalation (`bumpSeverity`, capped) are implemented and tested (`gap.ts:139-142`, `gap.test.ts` "caps escalated severity at high"). Missing: heat computation from calibration/drill misses, severity triage UI, per-topic scoping, and feeding `predictReadiness` (tests-engine exists but is not fed by gap signal). |
| GAP-12 | 🟡 Partial | `gapsOf(db, userId)` already returns gaps across all topics for the learner (cross-topic canonical records). Missing: global board UI, heat/status/recency sort, bulk triage filters, normalized cross-topic heat score, partial-load handling. |
| GAP-13 | 🟡 Partial | "Where to focus" section exists as a static block on `app/reports/page.tsx:152-154`. Missing: items are not clickable, not wired to gap records, no tag↔severity mapping, no "create/recreate gap" affordance. |
| GAP-14 | ⏭️ Deferred | Instructor cohort-aggregate view needs Teams/org multi-learner data and the ORG-ADMIN progress-visibility policy (Teams/SSO/SCIM are deferred infra). RBAC 12-role matrix (`lib/domain/rbac.ts`) provides the read-only/no-write seam, but no aggregation, minimum-cohort thresholds, or instructor view are built. |
| GAP-15 | 🟡 Partial | Per-learner gap ownership is real and tested: `GapRecord.userId` + `gapsOf` scoping never leaks across users (`lib/store/db.ts:63`, `store.test.ts`), and gaps reference claim ids read-only (engine never touches trust state). Missing: shared-library membership, inherited read-only trust display in a gap, "underlying content updated" handling — the Teams shared-library flow is not built. |
| GAP-16 | 🟡 Partial | The engine keeps `reopened` a distinct status (not color/position alone) and announces transitions via history, supporting the a11y contract at the data layer (`gap.ts:45-58`). Missing: no board exists, so keyboard operation, semantic status/severity labels+glyphs, managed focus, announcements, and single-column reflow are not built. |
| GAP-17 | ⏭️ Deferred | Support restore-only recovery needs pre-incident snapshot/backup infrastructure and a scoped-consent support tooling flow (no real DB yet; in-memory store). RBAC provides audited break-glass as the seam (`lib/domain/rbac.ts`), but no snapshot/restore or restore-only firewall is implemented. |
| GAP-18 | ⏭️ Deferred | DSAR export, retention windows, and deletion propagation need data-governance infra (real DB, export pipeline). Privacy screen lists "Gap map & discussion threads" as exportable (`app/settings/privacy/page.tsx:44`, static). No export/retention/minor-safety enforcement implemented. |
| GAP-19 | 🟡 Partial | The engine meaningfully implements "never fabricate a false state": every illegal/stale transition is a pure no-op returning the same gap, history is append-only and grows monotonically (`gap.ts:176-185`; `gap.test.ts` "history grows monotonically", "illegal transition no-op"). Missing (needs real DB/client): offline queue with pending states, etag/optimistic-concurrency guards, and reconcile-on-focus badges. |
| GAP-20 | 🟡 Partial | "Reset gap map" surfaced with an irreversible-framed control on `app/settings/danger/page.tsx:43-49` (static). Missing: no wired reset action, no transactional delete of gaps+threads, no return-to-empty-state, no audit/recovery-window record. |
| GAP-21 | 🟡 Partial | `conflict` origin modeled and the seed creates a `from-dispute` gap (`lib/store/seed.ts:177`, high severity, conflict origin). Missing: dispute-resolution→gap creation not wired (Conflicts tab is static), and upstream conflict-reopen→gap-reopen not wired; no conflict-resolution-history link. |
| GAP-22 | ⏭️ Deferred | Notification delivery is owned by the Notifications domain and needs delivery/preferences infra (push/email deferred); Notifications page is static. Seam exists: `gradeCard` already emits a `gapReopened` signal (`review.ts:49`) that a delivery layer could consume. No batching/throttling or delivery built. |
| GAP-23 | ⏭️ Deferred | Marked Future in the PRD itself: requires a guardian↔dependent account relationship (Auth/Org) that does not exist in the single-account model. Needs account-model changes + minor-safety policy before any read-only guardian gap view is possible. |

**Summary:** 23 stories — ✅ 1 Done · 🟡 16 Partial · ⏭️ 6 Deferred · 🚫 0 Out-of-scope.
