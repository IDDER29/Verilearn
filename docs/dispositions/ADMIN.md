### Platform Admin, Moderation & T&S (ADMIN)

| Story | Status | Evidence / Justification |
|---|---|---|
| ADMIN-01 | 🟡 Partial | Data-isolation primitive exists: `topicsOf`/`reviewCardsOf`/`gapsOf` in `web/lib/store/db.ts` filter by `ownerId` and never leak across users (store tests). RBAC has `org_admin` + `tenant:provision` perm (`web/lib/domain/rbac.ts`). Missing: no tenant entity/registry, no provisioning flow, no seeded-role wiring, no SSO/IdP health (SSO itself is deferred infra), no atomic-rollback/collision handling. |
| ADMIN-02 | 🟡 Partial | Six-stage engine + fixed stage order in `web/lib/domain/pipeline.ts` (`PIPELINE_STAGES`, tested); `/pipeline` route animates a single run; `pipeline:operate` perm in RBAC. Missing the ops control plane: no per-stage queue depth/age (p50/p95), error rate, worker health, no scale/pause/re-queue/kill controls, no idempotent re-queue or poison-job dead-lettering. |
| ADMIN-03 | ⏭️ Deferred | Real execution sandbox is explicitly deferred (DeterministicVerifier stands in). Seam exists: the `Verifier` port in `pipeline.ts` is where a network-isolated, resource-bounded, ephemeral, fingerprinted sandbox adapter plugs in. Needs real container/isolation infra + adversarial-payload suite. |
| ADMIN-04 | ⏭️ Deferred | Needs a real judge/model + feature-flag rollout infra. Seams present: `Verifier` port swaps adapters, `flags:manage` perm in RBAC, and `trust.ts` verified%/breakdown aggregate is a substrate for trust-state-distribution diffing. Shadow run, staged ramp, one-click rollback, and T&S/SME co-sign gating are not implemented. |
| ADMIN-05 | ✅ Done | Fully implemented and tested in `web/lib/domain/rbac.ts` (`rbac.test.ts`, part of the 196 engine tests): 12-role non-overlapping capability matrix, `can`/`canWriteTrust` with no self-escalation, `trust:write` in `FIREWALL_PERMISSIONS` granted to no role and load-time-asserted (`assertNoEpistemicWriteGrants`), platform_admin/trust_safety_lead capability sets exclude epistemic write, and audited `breakGlass` returning immutable records with reason + session watermark + second-approval flag, refused for firewall permissions. |
| ADMIN-06 | ⏭️ Deferred | Depends on the real execution sandbox (ADMIN-03, deferred). No kill-switch, runner isolation, auto-freeze of `Verified·execution` claims, or incident/blast-radius record exists. `integrity:quarantine` perm and the trust-state model provide the eventual freeze target, but the containment machinery needs real sandbox infra. |
| ADMIN-07 | 🟡 Partial | One entitlement is enforced in code before compute: the Free 3-topic cap in `web/lib/services/topics.ts` (checked against `user.plan` before running the pipeline). Missing the system-level catalog: no tier→entitlement map (thoroughness, hard-mode Skeptic gate, seats, shared-library, SSO), no versioning/audit, no propagation to reading surfaces, no fallback-on-load-failure. |
| ADMIN-08 | 🟡 Partial | `community_mod` role + `community:moderate` perm defined in `rbac.ts`; Community surface exists as static UI (`app/community`). Missing the moderation queue itself: no reported/flagged item model, no hide/remove/warn/dismiss actions, no moderation audit log, no notify-with-rule, no "needs a claim link" flag, no brigade de-dup. |
| ADMIN-09 | 🟡 Partial | `community:moderate` perm scopes conduct enforcement; the firewall design (mod cannot judge citation quality) is honored by RBAC (mod lacks `source:promote`/`review:submit`). Missing: no cite-your-source flag, no warn→rate-limit→escalate ladder, no question/study-group exemption logic, no offline-source routing. |
| ADMIN-10 | 🟡 Partial | Firewall correctly modeled: `source:promote` is SME-gated (`sme_reviewer` holds it; `community_mod` does not) in `rbac.ts`, so routing can only ever be a request, not a write. Missing the actual routing/nomination flow: no nominate action, no "promotion requested" thread state, no authorship footprint, no SME-reject/moot handling. |
| ADMIN-11 | 🟡 Partial | Firewall boundary (local mod vs platform-wide T&S) is expressed in RBAC role separation (`community_mod` vs `trust_safety_lead`). Missing: no vote/leaderboard entity model, no vote-voiding action, no burst/self-promo detection, no ring escalation, no nomination-count reversal. |
| ADMIN-12 | 🟡 Partial | Design invariant ("wrong" is not a moderation reason; protect cited good-faith pushback) is documented and consistent with the firewall, but not implemented: no report taxonomy, no report-volume de-dup/threshold, no "possible good-faith dispute" surfacing, no debate reclassification routing. |
| ADMIN-13 | 🟡 Partial | Substrate exists: `signals.ts` (honest signals with none/low/ok confidence, no vanity fields) and `calibration.ts` (Brier + insufficient-data state) provide the distributions monitors would read. Missing the anomaly-detection layer: no confidence-gate cohort monitors, vote/account-graph analysis, Conflict-flood detection, evidence-carrying alerts, or scoped signal-voiding. |
| ADMIN-14 | 🟡 Partial | `integrity:quarantine` perm (T&S) in RBAC; the firewall guarantees only SME/system can resolve a state (`trust.ts`); tests already exclude Disputed claims (`tests-engine.ts eligibleClaims`), the exact seam quarantine would reuse. Missing: no "quarantined — under review" state, no hide/down-weight/block-new-certs effect, no SME review item, no reason/impact audit, no SLA/aging alert. |
| ADMIN-15 | 🟡 Partial | Strong engine coverage in `web/lib/domain/certificates.ts` (`certificates.test.ts`): `revokeCertificate` records reason+timestamp, `verifyCertificate` is fail-closed on revoked/undefined, `shouldRevokeOnClaimDowngrade` models downgrade propagation, and `cert:revoke` perm (second-approval gated) in RBAC. Missing the propagation surface: no public verify-URL page wired, no "cert revoked" webhook, no learner/ORG-ADMIN notification, no immutable revocation log or reinstatement (webhook/email are deferred infra). |
| ADMIN-16 | 🟡 Partial | `user:ban` perm held only by `trust_safety_lead` and second-approval-gated via break-glass (`rbac.ts`); PLATFORM-ADMIN correctly lacks it (holds infra off-switch, not ban authority). Missing: no ban/suspend/restrict state or enforcement, no illegal-content removal, no cross-tenant application, no notify+appeal, no freeze-instead-of-delete for depended-on content. |
| ADMIN-17 | 🟡 Partial | Input-defense implemented and tested in `pipeline.ts`: `sanitizeTopicInput` neutralizes prompt-injection (VERIFY-19) and `sourceHallucinationGuard` fails a fabricated citation closed to Unsupported (never `Verified·source`). Missing the T&S containment workflow: no quarantine-on-detection, no PLATFORM-ADMIN coordination item, no pattern-signature store, no route-to-SME, no cert-flag-for-review. |
| ADMIN-18 | 🟡 Partial | Punitive perms (`user:ban`, `cert:revoke`, `integrity:quarantine`, `user:impersonate`, `tenant:provision`) require a distinct second approver via `SECOND_APPROVAL_PERMISSIONS` and break-glass demands a non-empty reason (`rbac.ts`). Missing: no evidence-of-coordination/impossibility threshold, no "high-volume auditing is non-actionable" rule, no appeal channel with target response time. |
| ADMIN-19 | 🟡 Partial | Reputation↔honest-signal firewall holds by construction: `signals.ts` exposes only Retention/Transfer/Calibration/BlindSpot with no reputation/vanity inputs, so social standing cannot leak into learning signals. Missing the other half: no reputation/leaderboard model, no voiding action, no promotion-freeze-and-route-to-SME on a voided ring. |
| ADMIN-20 | 🟡 Partial | Break-glass emits immutable `BreakGlassAudit` records (actor/permission/reason/timestamp/session watermark/granted/forbidden), tested in `rbac.test.ts`; fail-closed-on-accountability spirit matches the ledger's fail-closed rendering. Missing a general audit facility: no append-only/tamper-evident persistent store, no query-by-target/actor/action API, no logging of ban/revoke/quarantine/consented-access (those actions don't exist yet), no buffer-then-flush on sink outage. |
| ADMIN-21 | 🟡 Partial | Revocation≠deletion is modeled as distinct actions: integrity revoke lives in `certificates.ts` (`revokeCertificate`), deletion is the privacy lever (`app/settings/danger`, static), and `dpo:manage`/`compliance:export` perms sit with `compliance_dpo` in RBAC. Missing: no collision detection between a fraud hold and an erasure request, no DPO disposition routing, no "retained under legal obligation" outcome. |
| ADMIN-22 | 🟡 Partial | Reinstatement target exists on the cert model (revoke is non-mutating and `verifyCertificate` is status-driven, so a "reinstated" flip is reachable), and second-approval gating anticipates dual-actor review. Missing the actual recovery path: no appeal channel/target response time, no reinstate action restoring organic-only reputation, no re-propagated "reinstated" state, no restore-from-checkpoint, no epistemic-complaint routing to SME. |
| ADMIN-23 | 🟡 Partial | Honest-degradation primitives are in place: nothing invents a `Verified` — `verifyCertificate` and `sourceHallucinationGuard` fail closed, and certs are gated on a real pass (`certificates.ts`, `tests-engine.ts`). Missing the degraded-mode surface: no explicit "verification delayed / in progress" state, no public system-status page or independent fallback channel, no hold-then-drain-on-recovery with learner notification. |

Counts: total 23 — ✅ Done 1, 🟡 Partial 19, ⏭️ Deferred 3, 🚫 Out-of-scope 0.
