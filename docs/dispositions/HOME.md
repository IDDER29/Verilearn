### Learner Home / Dashboard & Discovery (HOME)

| Story | Status | Evidence / Justification |
|-------|--------|--------------------------|
| HOME-01 | 🟡 Partial | First-run screen exists as a faithful static page (`app/welcome/page.tsx`): branded hero "Learn things you can actually trust", 3-step "How VeriLearn works" strip, example on-ramps, and `0` stat placeholders with "These fill in once you start your first topic." Missing: it is **not routed/wired** — the real dashboard (`app/page.tsx`) shows an inline "No topics yet" empty state and never redirects to `/welcome`; first-run detection (per-account ≥1 topic in any stage, failed-topic override) is not implemented. |
| HOME-02 | 🟡 Partial | Curated examples (Dijkstra, Merkle trees, Binary search) with subject+level render on `app/welcome/page.tsx`. Missing: each example is a plain `href="/new-topic"` — it does **not** seed a pre-filled New Topic or open a pre-verified showcase; no demote-once-you-have-topics logic (static, unwired). |
| HOME-03 | 🟡 Partial | `app/page.tsx` header aggregates **real** counts: `dueCount` from `reviewCardsOf`/FSRS (`c.fsrs.due <= now()`) and `conflicts` from the trust ledger (sum of `t.disputes` via `listTopicSummaries`). Missing: tasks-due and newly-verified counts are not included; the counts in the sentence are not individually links to Review/Conflicts/Tasks; no explicit "all caught up" reward state with next-due date; timezone/day-boundary logic is `now()`-based only. |
| HOME-04 | 🟡 Partial | A "To review" priority panel exists in the right rail of `app/page.tsx` but is **fully hardcoded static** ("4 flashcards due", "Resolve 2 conflicts", "Finish 1 task"). Missing: cross-portfolio aggregation, deterministic urgency ordering (overdue > due-today > soon), real due context, "+N more" cap, and "see all" into My Tasks. |
| HOME-05 | ✅ Done | Wired to real data. `app/page.tsx` renders each topic row from `listTopicSummaries(user.id)` (`lib/services/topics.ts` `summarize`): title, level, claim count, segmented `TrustBar` from the ledger `trustBreakdown` (verified/sourced/disputed), `% verified` text, and a status chip ("N disputes"/"Verified"/"On track") derived from real ledger state; "See all" → `/topics`. Segments/percent come from the trust ledger's states (single source of truth). |
| HOME-06 | 🟡 Partial | A search input ("Search topics…") is present in the `app/page.tsx` top bar, but it is an inert `<input>` — no debounced typeahead, no action, no results, no scoping applied at the query. Topic data is tenant-scoped in the store (`topicsOf`), so the seam exists, but search itself is unbuilt. |
| HOME-07 | ⏭️ Deferred | No claim-level trust-state search. The underlying data exists (per-claim `TrustState` via the ledger, `claimStates` in `getTopicView`), but there is no search index/service over decomposed claims and no filter/operator UI. Needs a tenant-scoped search index (Key technical requirement); ledger trust states are the ready data source seam. |
| HOME-08 | 🟡 Partial | An "Upcoming tests" widget with urgency badge and "see all" → `/tests` renders in `app/page.tsx`, but every entry is **hardcoded static** (Dijkstra "in 2 days", Merkle "18 Jul", Binary search "24 Jul"). Missing: real scheduled tests, time-to-deadline, and predicted-readiness (the tests engine `predictReadiness` exists in `lib/domain/tests-engine.ts` but is not read here). |
| HOME-09 | 🟡 Partial | A **real** open-conflicts count reaches Home via the header line (`conflicts` = ledger disputes summed across topics). Missing: the "Resolve 2 conflicts" rail item is hardcoded; no dedicated conflicts widget carrying most-urgent topic/claim context; no zero-state reassurance ("Every claim is currently settled"); no `/conflicts` route to shortcut into (Conflicts UI is a separate static surface). |
| HOME-10 | 🟡 Partial | The data model supports it: `createTopic` sets `status` to `"ready"` or `"verifying"`, and the pipeline is a 6-stage `runPipeline` (`lib/domain/pipeline.ts`). Missing on Home: no live "Verifying… (stage N of 6)" status render, no non-enterable gating of in-progress topics, and no failed-state/retry cue in the topic list (the `/pipeline` page is a separate animated static screen; pipeline runs synchronously so topics land "ready" immediately). |
| HOME-11 | 🟡 Partial | The three stat cards on `app/page.tsx` ("Verified topics 24", "Claims checked 128", "Certificates 10") are **hardcoded literals** — not traceable, not derived, and thus in tension with the no-vanity thesis. The real data exists (topic/claim counts via `listTopicSummaries`; `lib/domain/certificates.ts` issue/verify), and the first-run `welcome` page does show honest `0` placeholders, but the populated dashboard counters are not wired to real records. |
| HOME-12 | 🟡 Partial | Engine seam exists: `lib/domain/rbac.ts` defines a read-only `guest` role (topic:read/claim:read true; topic:create/test:take/trust:write false — `rbac.test.ts`). Missing: no ephemeral demo dashboard route/render path, no no-persistence guest home, no vendor-curated demo topic, and no write→sign-up gating UI. |
| HOME-13 | 🟡 Partial | The Free 3-active-topic cap is enforced in the service before compute (`lib/services/topics.ts` `createTopic`: `user.plan === "free" && existing.length >= 3` → error), surfaced via `createTopicAction` and the New Topic form. Missing on Home: no plan-aware "Go Verified Pro" upgrade nudge, and the dashboard "Start a topic" CTA (`href="/new-topic"`) does not communicate the cap or route at-cap Free users to Upgrade; no Pro/Teams suppression of Free messaging. |
| HOME-14 | ⏭️ Deferred | No shared/team library. The store is strictly OWNER-scoped (`topicsOf` never crosses users); there is no Org-Admin shared-library model, inherited-trust surfacing, or team-owned/non-deletable distinction. Needs the Org-Admin shared-library + membership/visibility model (cross-domain). |
| HOME-15 | 🟡 Partial | Partial non-color encoding: trust bars carry a text "`N% verified`" figure and status chips carry text labels ("N disputes"/"Verified"/"On track") alongside color (`app/page.tsx`). Missing: no per-segment screen-reader text (e.g. "83% verified, 1 disputed"), the `TrustBar` component is decorative divs without ARIA, no verified semantic landmarks/heading structure, keyboard focus order, or WCAG 2.2 AA audit; live-region announcements for updating counters absent. |
| HOME-16 | ⏭️ Deferred | No offline support: no cached last-known counters/topic list, offline banner, "Try again", or offline-review sync-queue. The store is in-memory server-side with no client cache/service-worker layer. Needs PWA/offline caching infra (and a real network layer, itself deferred). |
| HOME-17 | ⏭️ Deferred | No per-widget load/error boundaries or per-source timeouts; the dashboard renders synchronously from a single in-memory store, so there is no multi-service fan-out to degrade independently. Needs the real multi-service backend (DB + verifier + tests/conflicts services) — itself deferred — before independent widget resilience is meaningful. |
| HOME-18 | ⏭️ Deferred | No no-results / search-error experience, because search (HOME-06/07) is not wired. Needs the search service + client-side fallback filtering path to exist first; no seam beyond the inert input. |
| HOME-19 | 🟡 Partial | Reconcile-on-reload works implicitly: the dashboard is server-rendered and recomputes `dueCount` and `conflicts` fresh from the source-of-truth (FSRS store / ledger) on every request, and `createTopicAction` calls `revalidatePath("/")`, so counts never serve stale cached numbers and are naturally non-negative. Missing: no visibility-change/focus live reconciliation, no push/poll, no optimistic-decrement correction. |
| HOME-20 | ⏭️ Deferred | Priority Future; self-described documented gap. Current single-shared-account behavior (a guardian would see exactly the learner's Home) matches acceptance criterion #1 by default, but there is no distinct guardian role or oversight surface. Needs a guardian role model routed through the Compliance/DPO (FERPA/COPPA) domain — a business/product decision. Age-gating groundwork exists in `lib/auth/age.ts`. |
| HOME-21 | ⏭️ Deferred | No "learn next" suggestions row on Home. Curated example data exists (`app/welcome/page.tsx`) as a potential fallback, but no suggestions surface, ranking, or pre-filled routing is built. Nice-to-Have, unbuilt; needs a cached/cheap suggestion source. |
| HOME-22 | 🟡 Partial | Invariant holds: Home exposes no trust-mutation/certify control to anyone (no such UI exists), and `lib/domain/rbac.ts` defines an `instructor` role with pedagogical-not-epistemic permissions (no `trust:write`). Missing: no role-differentiated Home — `AppShell` renders one learner-oriented nav; there are no instructor cohort/assignment/reporting entry points surfaced by role. |

**Counts:** 22 total — ✅ 1 Done · 🟡 14 Partial · ⏭️ 7 Deferred · 🚫 0 Out-of-scope.
