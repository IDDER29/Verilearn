### Progress, Reports & Analytics (ANALYTICS)

Evidence base: the honest-signals engine `web/lib/domain/signals.ts` (pure, tested — `signals.test.ts`, 12 provenance assertions), the calibration engine `web/lib/domain/calibration.ts` (direction + insufficient-data, tested), the wired service `web/lib/services/progress.ts` (`progressFor` → real review log, graded tasks, calibration), and the Reports UI `web/app/reports/page.tsx`. The RBAC firewall (`web/lib/domain/rbac.ts`, `trust:write` granted to no human role) is the structural anti-fabrication seam. No cohort/org/visibility-policy/export/DSAR/anti-gaming/support-restore code exists anywhere under `web/` (confirmed by grep).

| Story | Status | Evidence / Justification |
|-------|--------|--------------------------|
| ANALYTICS-01 | 🟡 Partial | Four signal cards render real values via `progressFor` → `computeSignals` (`app/reports/page.tsx` L47-68), each with a plain-language definition and a source-naming provenance string (`signals.ts` L150-153: "retention ← FSRS review recall", etc.); no vanity metric at signal weight (the `Signals` interface has exactly four keys). **Missing:** the "short-window trend / directional delta (▲4%)" — no delta is computed or shown; the card sub-line is a static "On track"/"Low confidence" label, not a windowed delta. |
| ANALYTICS-02 | 🟡 Partial | Provenance exists in the engine: every `Signal` carries a `provenance` trace naming what fed it and the counts (`signals.ts` L42-48, L119-140), and `signals.test.ts` asserts it. **Missing:** no UI drill-in — clicking a card opens no breakdown, no window/sample/formula panel, no navigation to the contributing artifact. The data is there; the explainability surface is not built. |
| ANALYTICS-03 | 🟡 Partial | A By-topic table with Retention/Transfer/Calibration bars and a Verified trust-coverage column is present (`app/reports/page.tsx` L156-239). **Missing:** every row is hardcoded (Dijkstra 81/68/88/83%, Merkle, Binary search); it is not wired to per-topic real signals, is not sortable, and rows do not link into the topic workspace. Per-topic signal aggregation does not exist in `progress.ts` (which aggregates across all topics). |
| ANALYTICS-04 | 🟡 Partial | A "Where to focus" rail with ranked, reasoned rows and a positive "solid · nicely calibrated" state is present (`app/reports/page.tsx` L112-153). **Missing:** all three entries are hardcoded (Merkle/Hashing/Binary search); no real ranking from signal values, no links to a Gap Map entry / due Review / weak section, no healthy-state fallback logic. |
| ANALYTICS-05 | 🟡 Partial | A retention/transfer line chart with legend, a "Now" endpoint, week buckets and a range control is laid out (`app/reports/page.tsx` L72-110). **Missing:** the chart is a static hardcoded SVG path; the "Last 30 days" range selector is a non-functional button; no real time-bucketed data, no shared-range re-scoping of cards/breakdowns, no "not enough history" state. |
| ANALYTICS-06 | 🟡 Partial | The calibration engine computes and names direction — `overconfident` / `underconfident` / `well_calibrated` with a threshold band and per-bucket accuracy (`calibration.ts` L26-27, L125-145), tested, and the aggregate direction flows into the signal provenance (`signals.ts` L138). **Missing:** no per-topic calibration report UI, no over/under split shown to the learner, no links to seeded error-drills; the top-line card shows a score, not a direction. |
| ANALYTICS-07 | 🟡 Partial | The blind-spot signal is a first-class field defined as seeded-error catch rate (`signals.ts` L75-76, L153, provenance "blind-spot ← seeded error-drill catch rate"), with honest empty handling. **Missing:** `progress.ts` L31 passes `drills: []` explicitly — seeded error-drills are not wired, so the signal is always empty; no per-topic blind-spot breakout, no drill links, no memorization flag. |
| ANALYTICS-08 | ✅ Done | Honest empty state is implemented, tested, and wired: `computeSignals` returns `value: null` / `confidence: "none"` with a "no data yet" provenance for any empty source (`signals.ts` L105-121), never a fabricated 0%; `signalDisplay` renders "—" / "Not enough data yet" and the card shows a "new" chip (`progress.ts` L38-42, `app/reports/page.tsx` L8-12, L49-63); `signals.test.ts` covers the empty case. Note: no dedicated first-run explainer/CTA routing beyond the per-card sub-labels. |
| ANALYTICS-09 | ✅ Done | Low-confidence flagging is implemented and tested: `LOW_CONFIDENCE_SAMPLE = 5` with a `bandFor` low/ok band kept in step with calibration's `MIN_RECORDS` (`signals.ts` L99, L110-113; `calibration.ts` L44, L96-98), the value is shown with a "low" chip and "Low confidence · keep going" caveat (`progress.ts` L41, `app/reports/page.tsx` L9-11), and confidence rises to "ok" once the sample crosses the threshold. The "exclude low-confidence from Where-to-focus ranking" clause is moot while that rail is static (see ANALYTICS-04). |
| ANALYTICS-10 | ⏭️ Deferred | Cohort view for instructors is not built — no cohort entity, no multi-learner aggregation, no read-only cohort dashboard. Teams-gated (R2/R3 phasing). Seam: `computeSignals` is the pure aggregator that a cohort roll-up would reuse, and the RBAC matrix already denies instructors `trust:write`. Needs a cohort/shared-library data model + Teams plan. |
| ANALYTICS-11 | ⏭️ Deferred | Policy-gated per-learner drill-down does not exist — there is no progress-visibility policy model, no server-side visibility enforcement, no audit log of per-learner exposure. Needs the org visibility-policy model + Teams cohort scope. |
| ANALYTICS-12 | ⏭️ Deferred | Org-level ROI dashboard (aggregate signal movement + completion/certificate counts, team/date scoping) is not built. Certificates exist in the Tests/Certs domain to read from, but no org aggregation surface consumes them. Teams-gated; needs tenant roll-up + org dashboard. |
| ANALYTICS-13 | ⏭️ Deferred | Server-enforced per-learner privacy vs. a manager is not built — there is no manager role reading learner signals and no visibility-policy machinery. Store OWNER/TENANT scoping makes data private by default today, but the policy-exposure model, save-time compliance guard, and offboarding freeze the story requires do not exist. Needs the visibility-policy model + Teams. |
| ANALYTICS-14 | ⏭️ Deferred | Export/share of a progress report with scoped, revocable consent is not built — no export action, no share link, no consent model, no revocation propagation. (The Reports "Last 30 days" button is inert and unrelated.) Needs a share-link + consent/revocation model. |
| ANALYTICS-15 | ⏭️ Deferred | T&S anti-gaming detection (calibration-juking, drill memorization, streak-farming, vote-rings) with void/quarantine/freeze-then-recompute is not built — no statistical monitors, no quarantine action. The epistemic firewall is present as the seam (`rbac.ts`: `trust:write` in `FIREWALL_PERMISSIONS`, granted to no human role), and because signals are computed-not-stored, "recompute not overwrite" is structurally true; the detection/void tooling itself needs building. |
| ANALYTICS-16 | ⏭️ Deferred | Support checkpoint-restore of lost history under scoped consent is not built — no checkpoint store, no consent-gated restore action. The anti-fabrication half is enforced structurally: no role can write a signal value (signals are derived, and `rbac.ts` grants no signal/`trust:write` capability to Support). Needs checkpoint/consent infra. |
| ANALYTICS-17 | 🟡 Partial | Signal cards pair color with an always-present text label and value, so the four headline signals are not color-only (`app/reports/page.tsx` L59-65). **Missing:** grep finds no `aria-*`, no `role="img"`, no data-table equivalent for the trend chart, no `prefers-reduced-motion` handling; the chart and the red calibration bar convey meaning by color/position alone; delta arrows and keyboard-reachable range/sort/drill controls are absent (those controls are themselves static). |
| ANALYTICS-18 | ⏭️ Deferred | Signals-as-sensitive-personal-data governance (DSAR export including derived signal history, reset-history/deletion cascade to derived + aggregated data, retention windows, minor constraints) is not built — no DSAR export, no wired danger-zone reset (Settings danger zone is static per the domain summary). Needs DSAR/retention infrastructure and a deletion-cascade with aggregate recompute. |
| ANALYTICS-19 | 🟡 Partial | The four top cards are effectively a portfolio-wide roll-up: `progressFor` aggregates every review/task across all topics into one set of signals (`progress.ts` L17-35). **Missing:** healthy/watch/weak band counts, biggest-mover (most-improved/declined) callouts over a window, disclosed weighting, and links from a band into a filtered per-topic list — none exist. |
| ANALYTICS-20 | 🟡 Partial | Honest-failure-on-thin-data is done (empty / low-confidence, never a fabricated value — see ANALYTICS-08/09) and the engine never extrapolates. **Missing:** the freshness/staleness half — no "as of <timestamp>" snapshot indicator, no "couldn't refresh — showing values as of …" degraded card, no per-signal freshness marker, no total-outage error+retry state, no offline last-snapshot banner. Signals are recomputed inline per request with no snapshot/timestamp model. |
| ANALYTICS-21 | ⏭️ Deferred | Optional digest notifications on material signal movement are not built — the Notifications service produces no signal-change events (grep of `notifications.ts` for retention/calibration/signal/digest/material finds nothing), no debounce, no opt-in. Seam: a Notifications domain exists to consume material-change events; this domain must still emit them (plus transactional-email delivery is itself deferred infra). |

**Summary:** 21 stories — ✅ 2 Done · 🟡 10 Partial · ⏭️ 9 Deferred · 🚫 0 Out-of-scope.
