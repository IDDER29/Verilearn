### Progress, Reports & Analytics (ANALYTICS)

| Story | Status | Evidence / Justification |
|---|---|---|
| ANALYTICS-01 | 🟡 Partial | Four-honest-signals engine `computeSignals` in `web/lib/domain/signals.ts` (exactly retention/transfer/calibration/blindSpot, no vanity fields; each carries value + plain-language provenance) is unit-tested (`signals.test.ts` "shape: exactly four honest signals, no vanity fields (ANALYTICS-01)"). But `web/app/reports/page.tsx` is a faithful static screen with hardcoded values (74%/61%/82%/63%) NOT wired to the engine; directional deltas are decorative. Missing: wiring to real signals; window-based delta computation. |
| ANALYTICS-02 | 🟡 Partial | Each `Signal` carries a `provenance` string naming what fed it (tested "calibration carries the score and names the direction in provenance"). Missing: interactive drill-in breakdown UI, sample-size (n) + formula display, and per-input navigation to the concrete artifact (review card / task / test question). No breakdown route exists. |
| ANALYTICS-03 | 🟡 Partial | "By topic" table with Retention/Transfer/Calibration bars + Verified % is a faithful static screen in `web/app/reports/page.tsx` (rows hardcoded). Verified trust-coverage engine exists in `web/lib/domain/trust.ts` (verified% aggregates). Missing: wiring to real per-topic signals, sortability, and row links into the topic workspace. |
| ANALYTICS-04 | 🟡 Partial | "Where to focus" rail is a faithful static screen (`reports/page.tsx`, Merkle/Hashing/Binary-search cards with reasons). Missing: ranking derived from real signal values, links to genuine remediation targets (Gap Map / due Review), and the all-healthy state. |
| ANALYTICS-05 | 🟡 Partial | Static SVG retention/transfer line chart + non-functional "Last 30 days" range button in `reports/page.tsx`. Missing: real time-bucketed data, a working range control that re-scopes cards/breakdowns consistently, and the <1-window "not enough history" state. |
| ANALYTICS-06 | 🟡 Partial | Calibration *direction* is computed and tested — `web/lib/domain/calibration.ts` returns `CalibrationDirection` = overconfident/underconfident/well_calibrated (`direction` field). Missing: per-topic calibration report UI surfacing direction, links to error-drills, and the confidence-gate-disabled exclusion/disclosure. |
| ANALYTICS-07 | 🟡 Partial | Blind-spot signal implemented as seeded-error-drill catch rate (`signals.ts` blindSpot; tested "blindSpot = seeded-error-drill catch rate"). Static "Drill detection 63%" card exists. Missing: per-topic blind-spot breakout UI, "no drills yet" state, and links to weak-topic drills. |
| ANALYTICS-08 | 🟡 Partial | Honest empty state is in the engine: no inputs → `value:null`/`confidence:"none"` never a fake 0% (tested "honest empty state — no data → null/none (ANALYTICS-08)"). Missing: the first-run explainer UI and primer→low-confidence transition; the static reports page shows hardcoded numbers rather than an empty state. |
| ANALYTICS-09 | 🟡 Partial | Low-confidence flagging is implemented + tested — `LOW_CONFIDENCE_SAMPLE=5`, sparse samples reported with `confidence:"low"`, promoted to `ok` at threshold (`signals.test.ts` "low-confidence under the small-sample threshold (ANALYTICS-09)"). Missing: UI rendering of the caveat/sample size and exclusion of low-confidence signals from "Where to focus" ranking (no ranking wired). |
| ANALYTICS-10 | ⏭️ Deferred | No cohort aggregation engine or Instructor cohort view built; Teams-gated (needs multi-learner org + shared library). Epistemic-firewall seam exists: `web/lib/domain/rbac.ts` grants NO human role `trust:write`, so an instructor cannot edit a signal. Needs: cohort roll-up compute + Teams plan + anonymity-floor suppression. |
| ANALYTICS-11 | ⏭️ Deferred | No per-learner policy-gated drill-down. Store enforces OWNER/TENANT scoping (`web/lib/store`, tested never-leak) as a seam, but there is no ORG-ADMIN progress-visibility policy, server-side gate, or audit-log of per-learner exposure. Needs: Teams + org policy engine. |
| ANALYTICS-12 | ⏭️ Deferred | No org-level aggregate dashboard. Certificate issuance exists (`web/lib/domain/certificates.ts`) as a display source, but org-wide signal roll-up, completion/cert counts, and team/date scoping are not built. Teams-gated; needs multi-tenant aggregation. |
| ANALYTICS-13 | 🟡 Partial | Default-private baseline is enforced: the in-memory repository is owner/tenant-scoped so one learner's cards/gaps never leak to another user (`web/lib/store`, tested `reviewCardsOf`/`topicsOf` isolation). Missing: the ORG-ADMIN visibility policy, the learner-facing "what your org can see" view, and server-side exposure gating with save-time compliance block. |
| ANALYTICS-14 | ⏭️ Deferred | No progress export or shareable-report backend. Static privacy/consent screen exists (`web/app/settings/privacy`) as the seam. Needs: export generation, scoped/time-boxed revocable consent, and revocation propagation to live shares/API consumers (external infra). |
| ANALYTICS-15 | 🟡 Partial | Anti-fabrication half is enforced: `rbac.ts` firewall means T&S (and all human roles) lack `trust:write`, so no one can hand-set a signal to a chosen value. Missing: statistical gaming detection (calibration-juking, drill memorization, vote-ring), and void/quarantine/freeze actions that recompute aggregates. |
| ANALYTICS-16 | 🟡 Partial | Fabrication firewall enforced by the permission model — `rbac.ts` denies Support `trust:write` and default-denies impersonation, so Support cannot invent/set a signal. Missing: consent-scoped checkpoint-restore of lost report/history state, audit logging of the restore, and the dispute-routing decline path. |
| ANALYTICS-17 | 🟡 Partial | Signal cards carry text labels (name, value, delta %) alongside color. Missing (largely unaddressed on the static `reports/page.tsx`): a data-table/text-series equivalent for the SVG trend chart, non-color-only calibration flag (red bar has no "low/overconfident" text), `prefers-reduced-motion` static equivalents, and keyboard-reachable range/sort/drill controls (range selector is a non-functional button). |
| ANALYTICS-18 | ⏭️ Deferred | No DSAR export of derived signals, no verified deletion cascade, no retention-window enforcement. Static "Danger zone" reset screen exists (`web/app/settings/danger`) as the seam. Needs: real DB for retention/purge + governed export + cascade with correct aggregate recompute. |
| ANALYTICS-19 | 🟡 Partial | Building blocks exist (`computeSignals` + consistent low-confidence handling reusable across levels). Missing: the portfolio roll-up view itself — portfolio-wide averages with disclosed weighting, healthy/watch/weak band counts, biggest-movers, and single-topic degenerate handling. Nice-to-Have; not built. |
| ANALYTICS-20 | 🟡 Partial | Honest-failure core partially in the engine: signals refuse to fabricate (null/none empty, low-confidence caveat) rather than guess. Missing: snapshot/"as of" freshness timestamps, "couldn't refresh — values as of <t>" degraded cards, total-outage error+retry state, and offline last-snapshot banner. No snapshotting layer built. |
| ANALYTICS-21 | ⏭️ Deferred | No material-change event feed or digest opt-in. Static Notifications screen exists as the consuming surface. Owned by the Notifications domain; needs debounced above-noise change detection + delivery infra (transactional email/push, both deferred). |

**Counts:** 21 total — ✅ 0 Done · 🟡 15 Partial · ⏭️ 6 Deferred · 🚫 0 Out-of-scope.
