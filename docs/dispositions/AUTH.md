### Authentication, Onboarding & Identity (AUTH)

| Story | Status | Evidence / Justification |
|---|---|---|
| AUTH-01 | 🟡 Partial | No true no-login guest demo route exists — `proxy.ts` redirects every unauthenticated request to `/login` (only `/login`, `/signup` are public). The substrate a demo needs exists (ledger engine `lib/domain/trust.ts` with fail-closed rendering; deterministic seed with pre-verified demo topics; `/login` pre-fills a demo account for instant try), but there is no ephemeral, session-scoped, pre-auth guest surface and no live-vs-gated labeling. |
| AUTH-02 | 🟡 Partial | An auth wall exists but as a **blanket** gate, not placed at the persist boundary: `proxy.ts` + `requireUser()` (`lib/auth/current.ts`) redirect all app routes to `/login`. The Free 3-topic cap is enforced before compute (`services/topics.ts`). Missing: value-boundary placement, return-to-intended-action after auth, and carry-over of demo intent (typed topic name). |
| AUTH-03 | ✅ Done | `signUp` in `lib/auth/service.ts` (email normalize + per-scope uniqueness, ≥8-char policy, scrypt hash via `password.ts`, age-gate), wired via `signupAction` (`app/auth-actions.ts`) + `app/signup/page.tsx`; covered by `auth.test.ts` ("signs up an adult…", duplicate/invalid/weak/under-13 rejections). Gaps: duplicate-email message reveals existence (not fully anti-enumeration); no email-verification, Terms timestamp, disposable-domain reject, or per-IP sign-up rate-limit. |
| AUTH-04 | ⏭️ Deferred | No social/OAuth path implemented. Needs an external OAuth/OIDC provider integration; no adapter seam present beyond the local password credential adapter (`password.ts`). |
| AUTH-05 | ⏭️ Deferred | No email verification: accounts are created and usable without confirming email; `certificates.issue` is pass-gated but not verified-email-gated. Needs a transactional email pipeline + single-use expiring tokens. |
| AUTH-06 | ✅ Done | `signIn` + HMAC-signed tokens (`session.ts`, `signToken`/`verifyToken`), 30-day cookie (`setSessionCookie` in `current.ts`), `authenticate` re-checks signature+expiry+store membership, generic no-enumeration error, `signOut`; lands on Dashboard (`app/page.tsx` `requireUser` + real greeting). Tested in `auth.test.ts` (session tokens; sign-in uniform failure). Missing: "remember me", lockout/backoff counter, step-up re-auth. |
| AUTH-07 | 🟡 Partial | `app/welcome/page.tsx` is a faithful static first-run screen (hero, examples, "how it works") but not wired to real data — greets a hardcoded "Adeline", not role-aware, and has no shown-once logic. Dashboard (`app/page.tsx`) does greet the real user. Missing: real name/empty-state wiring, LEARNER-SELF vs LEARNER-TEAM branch, once-only gating. |
| AUTH-08 | ⏭️ Deferred | No password-reset flow (no request endpoint, token, or set-new-password screen). Needs transactional email + single-use expiring reset tokens; session-cascade primitive would build on the existing `db.sessions` store. |
| AUTH-09 | 🟡 Partial | `app/settings/profile/page.tsx` is faithful static UI (name, display name, avatar, language, timezone fields) with no Save action wired; the `User` entity exists in the store but edits don't persist. Missing: save/cancel persistence, timezone→FSRS recompute, cert-name audit log, naming policy. |
| AUTH-10 | ⏭️ Deferred | No change-email/re-verify flow. Needs transactional email (new-address verification + old-address security notice) and step-up re-auth. |
| AUTH-11 | ⏭️ Deferred | No MFA/TOTP or recovery codes. Needs an MFA vendor/TOTP implementation; step-up re-auth seam not yet present. |
| AUTH-12 | 🟡 Partial | Server-side revocation primitive exists — sessions are stored in `db.sessions` and `authenticate` denies any token absent from the store, and `signOut` deletes one; but there is no session-list UI (device/location/last-active) and no "sign out everywhere" / global-invalidation-on-reset cascade. |
| AUTH-13 | ⏭️ Deferred | No SAML/OIDC enterprise SSO. Needs an IdP integration + per-tenant config; RBAC role mapping seam (`lib/domain/rbac.ts`) exists to receive an IdP→VeriLearn role mapping. |
| AUTH-14 | ⏭️ Deferred | No seat-invite acceptance or shared-library landing. Needs the Teams tier + invite entity/multi-tenant provisioning infra; org roles (`org_admin`, `learner_team`) exist in the RBAC matrix as a seam. |
| AUTH-15 | ⏭️ Deferred | No org-admin provisioning UI/service (invite, assign role, deprovision, data-disposition). RBAC defines `org_admin` and the epistemic firewall (assigning INSTRUCTOR/SME grants no `trust:write`) as a seam, but no seat-ceiling/invite/audit provisioning flow. Needs Teams tier + Billing seat-ceiling dependency. |
| AUTH-16 | ⏭️ Deferred | Explicitly Future in the PRD. No SCIM directory sync or programmatic SSO config. Needs SCIM/SSO vendor integration; the "API cannot write the ledger" invariant is guaranteed by `FIREWALL_PERMISSIONS` in `rbac.ts`. |
| AUTH-17 | 🟡 Partial | Permission-layer primitive is implemented and tested: `support_agent` role holds only read/ticket perms; `user:impersonate` is granted to NO role by default (break-glass, audited via `breakGlass`), and the firewall blocks any support write to trust state (`rbac.ts`, `rbac.test.ts`). Missing: an actual consented/time-boxed support console, ticket-scoped access, and the resend/reset/delete-on-behalf actions. |
| AUTH-18 | 🟡 Partial | RBAC gives `trust_safety_lead` `user:ban` (and break-glass second-approval) while the firewall bars re-certification (`rbac.ts`, `rbac.test.ts`). Missing: account freeze/suspend/ban **state machine**, the immediate all-session invalidation cascade, appeal-path status screens, and cert-review referral on ban. |
| AUTH-19 | 🟡 Partial | `app/settings/danger/page.tsx` renders the exact typed-`DELETE`-to-confirm pattern with the destructive button disabled (static), but no delete action is wired — no account/content teardown, no session invalidation, no retention/disposition explanation. |
| AUTH-20 | ✅ Done | Isolation invariant implemented + tested: in-memory repository scopes by owner so `topicsOf`/`reviewCardsOf`/`gapsOf` never leak across users (`lib/store/`, 7+6 tests). Fail-closed authorization + audited break-glass in `rbac.ts` (default-deny; `platform_admin` excludes `trust:write` and epistemic reads). Note: scoping is per-owner; full multi-org tenant partitioning is lighter than the enterprise framing. |
| AUTH-21 | 🟡 Partial | The concrete age-gating criterion is done + tested: `ageGate` (`lib/auth/age.ts`) blocks under-13 pending parental consent, applies minor-safe defaults for 13–17, retains only a coarse age band; enforced in `signUp` and surfaced on the signup form (COPPA note). Tested in `auth.test.ts`. Missing (Future): first-class guardian↔dependent link and guardian dashboard. |
| AUTH-22 | 🟡 Partial | Auth pages have a11y basics — labeled inputs (`<label htmlFor>`), `autoComplete`, native input `type`/`required`/`minLength`, and an `ErrorNote` region (`app/login/page.tsx`, `app/signup/page.tsx`, `components/AuthShell`). Not verified WCAG-conformant: no confirmed aria-live error announcement, focus-to-first-invalid-field, or timed-challenge extend/retry (no MFA/CAPTCHA surfaces exist yet). |
| AUTH-23 | ⏭️ Deferred | Anti-abuse core absent — no sign-up/sign-in rate limiting, no CAPTCHA/progressive challenge, no disposable-domain policy list, no credential-stuffing lockout/breach-check. Sign-in anti-enumeration (`service.ts`) is the one facet present. Needs a rate-limit store + accessible-CAPTCHA vendor + breach-check API; no seam yet. |
| AUTH-24 | 🟡 Partial | Only blunt expiry handling exists: `authenticate` returns null on expiry and `proxy.ts`/`requireUser` redirect to `/login` — which loses work, the opposite of the story's intent. Missing: lightweight re-auth-and-resume, preservation of uncommitted work, transactional (never double-applied) commits, and server-side background-pipeline reattach. |

**Counts:** 24 total — ✅ 3 Done, 🟡 11 Partial, ⏭️ 10 Deferred, 🚫 0 Out-of-scope.
